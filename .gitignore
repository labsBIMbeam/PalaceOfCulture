# Dependencies
node_modules/
.pnpm-store/

# Builds
dist/
build/
*.tsbuildinfo

# Generated data — never commit (per global standards)
*.db
*.sqlite
*.sqlite3

# Secrets
.env
.env.*
!.env.example

# Python (services/world-agent)
__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/
.venv/

# Tooling caches
.turbo/
coverage/

# Avatar assets (CC0 Quaternius packs/presets, VRM/FBX test assets) — fetched/dropped in, never committed
apps/web/public/avatar/

# Media sample feeds (real PC2.0 RSS saved same-origin for the demo) — fetched, not committed
apps/web/public/feeds/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Godot web build synced into the site (artifact — rebuild via pnpm game:sync)
apps/web/public/game/
