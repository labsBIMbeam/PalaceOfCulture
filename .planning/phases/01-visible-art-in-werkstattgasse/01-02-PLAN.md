---
phase: 01-visible-art-in-werkstattgasse
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/web/src/meaningverse/onboardingStory.ts
  - apps/web/src/meaningverse/phase1Relay.ts
  - apps/web/src/ui/Phase1RelayOverlay.tsx
  - apps/web/src/scene/PalaceScene.tsx
  - apps/web/src/scene/KerniFamiliar.tsx
  - apps/web/src/frontend/frontend.css
  - apps/web/tests/phase1-presence-smoke.ts
autonomous: true
napplet_boundary:
  classification: native-world
  participating_napplets: []
  presentation: [diegetic-world, focused-overlay]
  registry_effect: none
  reason: "The under-ten-minute loop is continuous movement, spatial assembly, placement, lighting, and in-world feedback inside the existing Palace runtime."
  rejected_alternative: "A separate Napplet or browser-looking window would fragment embodied play and duplicate the existing application-truth and transport seams."
requirements:
  - ART-01
  - ART-02
must_haves:
  truths:
    - A builder enters through the canonical five-second video, three authoritative cards, and Walk in sequence before seeing an unobstructed Street glimpse and the fixed fictional Wire.
    - The Wire can be put away immediately without engagement, reopened optionally, and never depends on current or remote content.
    - Attentive presence is accepted after 36 seconds of foreground world attention, pauses during Wire reopen or tab suspension, never resets, and remains accepted thereafter.
    - Presence is legible through complementary environmental, subtitle, text, shape, sound, and reduced-effects cues without exposing elapsed time or turning presentation into truth.
    - Kerni remains a small peripheral companion at the Z1 bench and speaks the locked orientation line only after proximity plus explicit user interaction.
    - After the player explicitly acknowledges that one accepted orientation line, application truth records one bounded `RelayMemoryFragment` (`source: kerni-orientation`, `meaning: leave-one-small-useful-thing`); a separate explicit workbench choice `connect-with-others` / `Begin relay` records `RelayInspirationChoice`, and only those two accepted facts make the derived `relayAssemblyEligible` handoff true for Plan 03.
    - Kerni presentation can acknowledge a later accepted placement nonverbally but cannot authorize, generate, assemble, place, sign, publish, or complete application state.
  artifacts:
    - apps/web/src/meaningverse/onboardingStory.ts
    - apps/web/src/meaningverse/phase1Relay.ts
    - apps/web/src/ui/Phase1RelayOverlay.tsx
    - apps/web/src/scene/PalaceScene.tsx
    - apps/web/src/scene/KerniFamiliar.tsx
    - apps/web/src/frontend/frontend.css
    - apps/web/tests/phase1-presence-smoke.ts
  key_links:
    - Canonical onboardingStory data drives the existing intro and Phase1RelayOverlay copy without a second onboarding source.
    - PalaceScene converts visibility, focus, Wire state, and active-frame samples into bounded reducer actions; accepted reducer facts alone drive presence presentation.
    - Phase1RelayOverlay owns DOM focus while open and returns focus to the invoking world control before PalaceScene resumes world keyboard handling.
    - KerniFamiliar consumes proximity and accepted-presentation props while phase1Relay remains the sole transition authority.
    - Phase1RelayOverlay emits the explicit player acknowledgement and `connect-with-others` workbench choice intents; `phase1Relay` alone creates `RelayMemoryFragment` and `RelayInspirationChoice` facts and derives `relayAssemblyEligible` for Plan 03.
  prohibitions:
    - algorithmic or real feed
    - feed engagement gate
    - timer, numeric progress, progress bar, percentage, or objective marker
    - Kerni authority or presentation-driven completion
    - quest rail or duplicated onboarding system
    - fake peers, ambient people, ghost sessions, or fabricated social evidence
    - direct Nostr SDK use in scene or UI
    - new package or dependency installation
    - database migration, schema push, or persistence schema work
    - generic builder, catalog, grid, free placement, or broad social system
---

<objective>
Expand the accepted Phase-1 tracer backward through canonical entry, honest feed refusal, monotonic attentive presence, bounded Kerni interaction, and the explicit ART-01 memory-fragment/inspiration handoff while covering D-01 through D-14.

Purpose: Deliver the ART-01 entry/presence/encounter portion of the Werkstattgasse loop without coercive engagement, fabricated attention, or world-agent authority, while preserving the application-owned truth seam required by ART-02.
Output: Canonical onboarding and Wire presentation, a tested attentive-presence reducer and focus/visibility orchestration, bounded Kerni presentation, explicit `RelayMemoryFragment` and `RelayInspirationChoice` handoff state, phase-scoped accessibility styles, and a runnable presence smoke contract.
</objective>

<execution_context>
@/home/flx/.hermes/gsd-core/workflows/execute-plan.md
@/home/flx/.hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-CONTEXT.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-RESEARCH.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-PATTERNS.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-VALIDATION.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-UI-SPEC.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

- `apps/web/src/meaningverse/onboardingStory.ts` — the single canonical intro/tutorial vocabulary, retaining the approved intro and replacing obsolete enter-time connection framing with the Phase-1 continuation.
- `apps/web/src/meaningverse/phase1Relay.ts` — `AttentivePresenceState`, bounded `RelayMemoryFragment`, `RelayInspirationChoice`, derived `relayAssemblyEligible`, and pure feed/presence/Kerni/handoff transitions with monotonic accepted truth.
- `apps/web/src/ui/Phase1RelayOverlay.tsx` — `FictionalWire`, focus-safe dismissal/reopen, `KerniProximity`, dialogue, explicit `Begin relay` inspiration choice, subtitles, and `AccessibleStatus` surfaces.
- `apps/web/src/scene/PalaceScene.tsx` — bounded active-attention sampling, browser visibility/focus handling, world/DOM keyboard ownership, and accepted-state presentation orchestration.
- `apps/web/src/scene/KerniFamiliar.tsx` — peripheral suggestion-only visual props and one accepted-placement nonverbal response.
- `apps/web/src/frontend/frontend.css` — phase-scoped focus, overflow, subtitle, status, and reduced-effects behavior.
- `apps/web/tests/phase1-presence-smoke.ts` — direct `tsx` contract for intro, feed, threshold, interruption, suspension, focus, accessibility, and Kerni authority boundaries.

<tasks>

<task type="auto">
  <name>Task 1: Preserve canonical entry and add the fixed, immediately dismissible Wire</name>
  <files>apps/web/src/meaningverse/onboardingStory.ts, apps/web/src/ui/Phase1RelayOverlay.tsx, apps/web/src/scene/PalaceScene.tsx, apps/web/src/frontend/frontend.css, apps/web/tests/phase1-presence-smoke.ts</files>
  <read_first>apps/web/src/meaningverse/onboardingStory.ts, apps/web/src/ui/Phase1RelayOverlay.tsx, apps/web/src/scene/PalaceScene.tsx, apps/web/src/frontend/frontend.css, apps/web/tests/phase1-tracer-smoke.ts, .planning/phases/01-visible-art-in-werkstattgasse/01-CONTEXT.md (D-01, D-02, D-03, D-07), .planning/phases/01-visible-art-in-werkstattgasse/01-UI-SPEC.md (Surface contracts 1–2, populated and overflow considerations), .planning/phases/01-visible-art-in-werkstattgasse/01-PATTERNS.md (Canonical intro and DOM overlay patterns)</read_first>
  <action>
Keep `INTRO_SEQUENCE`, `introSkipTarget`, and the existing authoritative card data as the sole canonical entry source. Preserve the exact D-01 order: play `/intro.mp4` as the approved five-second decorative raccoon-to-Kerni beat, then show the three canonical cards in their current order, then expose `Walk in` only on card 3. Video failure, mute, reduced effects, or video skip may advance only to card 1; retain the three cards and their copy verbatim. Update only the obsolete post-intro `TUTORIAL`/`tutorialStageFor` continuation so entering no longer claims the room connects or starts a competing ship-part tutorial.

After `Walk in`, let movement/camera control begin immediately and keep the Street unobstructed for 2400ms before presenting `FictionalWire` per D-02. Render exactly the local fixed Wire contract per D-07: header `THE NEARBY WIRE`, badge `FICTIONAL`, and these three cards in this order: `WORKSHOP NOTE` / `The mushroom sorter has rejected one perfectly ordinary spoon.`; `STREET WEATHER` / `Copper dust after dusk. The framed windows remain opinionated.`; `LOCAL STATUS` / `A small machine is still humming beside a bench nobody reserved.`. Do not add a fetch, ranking, remote data source, avatars, reactions, counts, compose control, or current headline seam.

Make one large full-width `Put away` action continuously available under the cards and let `Escape` invoke the same dismissal while focus is in the Wire, with no scroll/read/reaction prerequisite per D-03. Provide a low-priority `Wire` reopen control. On open, move focus to the feed heading or the safe dismissal control and contain focus; on dismissal, release movement-key state and restore focus to the invoking world control without firing a world action. Keep `E` and movement handlers inert whenever a button, link, or overlay owns focus. At 1280×720 the three cards and action fit without required scroll; at 200% zoom the overlay scrolls internally, essential copy wraps without ellipsis, and no horizontal page scroll appears. Use the approved 44px target minimum, 48px large-action height, 2px gold focus outline/3px offset, soot/copper tokens, and immediate reduced-motion open/close treatment.

Create the direct `node:assert/strict` smoke file in the established `tsx` style. Assert canonical beat IDs/order, card count/copy, `Walk in`, 2400ms glimpse constant or source contract, exact Wire content, direct dismissal and optional reopen actions, and accessibility/focus/overflow hooks. Keep this local and deterministic.
  </action>
  <verify>
    <automated>pnpm --filter @600b/web exec tsx tests/phase1-presence-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
- D-01 is source-tested as video → exactly three canonical cards → `Walk in`, with fallback/skip never bypassing cards.
- D-02 is source-tested as a 2400ms freely walkable unobstructed glimpse before the Wire enters.
- D-03 is source-tested with an always-available `Put away` and equivalent in-Wire `Escape`, independent of scroll, read, reaction, or other engagement.
- D-07 is source-tested against the exact three calm fictional cards and absence of a network/current-headline data path.
- The populated 1280×720 state and 200%-zoom overflow state retain all essential copy/actions, internal modal scrolling, visible keyboard focus, and deterministic focus return.
  </acceptance_criteria>
  <done>The canonical intro remains authoritative and unchanged in order/copy, the fixed fictional Wire appears only after the free glimpse, and a keyboard-only builder can put it away immediately or reopen it without any engagement requirement.</done>
  <reversibility rating="reversible">The entry continuation, local overlay composition, and phase-scoped styles can be reverted without data migration or persistent-state conversion.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define monotonic attentive presence with interruption and suspension boundaries</name>
  <files>apps/web/src/meaningverse/phase1Relay.ts, apps/web/src/scene/PalaceScene.tsx, apps/web/src/ui/Phase1RelayOverlay.tsx, apps/web/src/frontend/frontend.css, apps/web/tests/phase1-presence-smoke.ts</files>
  <read_first>apps/web/src/meaningverse/phase1Relay.ts, apps/web/src/scene/PalaceScene.tsx, apps/web/src/ui/Phase1RelayOverlay.tsx, apps/web/src/frontend/frontend.css, apps/web/tests/phase1-presence-smoke.ts, .planning/phases/01-visible-art-in-werkstattgasse/01-CONTEXT.md (D-04, D-05, D-06), .planning/phases/01-visible-art-in-werkstattgasse/01-UI-SPEC.md (Presence choreography, truth matrix, accessibility), .planning/phases/01-visible-art-in-werkstattgasse/01-RESEARCH.md (Pitfall 1 and ART-01 test map)</read_first>
  <behavior>
- RED case 1: 35,999ms of valid attentive samples remains accumulating; the sample that reaches 36,000ms accepts presence; further valid samples cannot reverse or duplicate acceptance (D-04).
- RED case 2: feed states `foreground` and `reopened` add zero attentive time; returning the feed to `away` resumes from the prior accumulated value without reset (D-06).
- RED case 3: a hidden, blurred, suspended, negative, non-finite, or oversized resume-gap sample adds zero or only the explicitly bounded active-frame delta; no suspended wall-clock gap can reach the threshold (D-04, D-06).
- RED case 4: once accepted, feed reopen, tab hide, blur, and malformed elapsed actions leave `presenceAccepted` true and accumulated time nondecreasing (D-06).
- RED case 5: presentation cue changes, subtitles, audio completion, animation completion, DOM visibility, and Kerni actions cannot dispatch or imply reducer acceptance (D-05).
  </behavior>
  <action>
Use RED → GREEN → REFACTOR inside `phase1-presence-smoke.ts`. First add failing pure assertions for every behavior above, run the focused command and confirm the expected failures, then implement the smallest pure reducer changes and rerun green before cleanup.

Extend the Plan-01 reducer with `AttentivePresenceState` and explicit feed/attention actions using the Phase-1 feed vocabulary `glimpse | foreground | away | reopened`. Select the UI-approved threshold of exactly 36,000ms, which satisfies D-04's 30–45 second boundary. Store accumulated attentive milliseconds and an accepted boolean in application truth. Accept elapsed input only when the document/window is foreground-focused, the Wire is `away`, and the orchestrator reports a bounded active-frame sample; reject negative/non-finite values and clamp a valid sample to at most 250ms. Never derive a resume delta from `Date.now()` or a hidden-tab gap. `visibilitychange`, window blur, feed reopen, and focus transfer reset only PalaceScene's sampling baseline and pause accumulation; they do not decrement or reset reducer state. Once accepted, presence and its accumulated accepted value are monotonic for the phase run.

Explicitly classify and close the ART-01 spec-less probe as an interruption/attention-state edge: feed reopen pauses accumulation without reset; tab suspension cannot add attentive time; accepted presence is monotonic. Include exact-boundary, just-below-boundary, reopen/resume, hidden-tab/resume-gap, malformed delta, and post-acceptance tests. Treat browser focus/time observations as untrusted hints that may request bounded accumulation; only the reducer accepts truth.

Drive D-05 presentation only from reducer state: four quiet environmental plateaus at 0–8s, 8–20s, 20–30s, and 30–36s may adjust workshop mix, framed-window edge, machine/prop posture, bench/socket rim, and Kerni noticeability, followed by a persistent settled state. Pair them with the UI-SPEC subtitle text and static text/shape alternatives, default subtitles on when muted, and honor both OS reduced motion and the in-experience `Reduced effects` toggle. Do not render elapsed/numeric state or repeatedly announce accumulation; expose only the accepted terminal status through `AccessibleStatus`/`aria-live="polite"`, once. Lighting, audio, subtitle, animation, DOM, and CSS callbacks remain outputs and never dispatch acceptance.
  </action>
  <verify>
    <automated>pnpm --filter @600b/web exec tsx tests/phase1-presence-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
- D-04 is proven at 35,999ms and 36,000ms, within the locked 30–45s range, for both walking and quiet foreground presence.
- D-06 and the ART-01 spec-less edge are proven: reopening pauses, putting away resumes the prior value, suspension adds no wall-clock gap, and accepted truth never reverses.
- D-05 is source-tested for subtitle, mute, reduced-effects, non-color text/shape, and persistent settled cues, while presentation cannot dispatch acceptance.
- No elapsed number, countdown, percentage, progress meter, checkpoint/objective marker, or repeating live-region announcement is presented.
- Focus/time authority remains bounded: DOM/modal ownership pauses world controls, tab/window loss resets the sampling baseline, and malformed or forged elapsed input fails closed.
  </acceptance_criteria>
  <done>The pure reducer and focused smoke prove exactly 36 seconds of genuine foreground attention, interruption-safe monotonic acceptance, suspension-safe sampling, and accessible accepted-state presentation without instrumentalizing presence.</done>
  <reversibility rating="reversible">The bounded reducer fields/actions and scene sampling policy are phase-local and introduce no persisted schema; threshold choreography can be adjusted later without rewriting accepted external data.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Bound Kerni and create the explicit memory-fragment/inspiration handoff</name>
  <files>apps/web/src/meaningverse/phase1Relay.ts, apps/web/src/ui/Phase1RelayOverlay.tsx, apps/web/src/scene/PalaceScene.tsx, apps/web/src/scene/KerniFamiliar.tsx, apps/web/src/frontend/frontend.css, apps/web/tests/phase1-presence-smoke.ts</files>
  <read_first>apps/web/src/meaningverse/phase1Relay.ts, apps/web/src/ui/Phase1RelayOverlay.tsx, apps/web/src/scene/PalaceScene.tsx, apps/web/src/scene/KerniFamiliar.tsx, apps/web/src/frontend/frontend.css, apps/web/tests/phase1-presence-smoke.ts, .planning/phases/01-visible-art-in-werkstattgasse/01-CONTEXT.md (D-08 through D-14 and ART-01 canonical encounter-to-memory reference), .planning/phases/01-visible-art-in-werkstattgasse/01-UI-SPEC.md (Kerni proximity, copy, focus, reduced-effects, Application Truth vs Presentation State), .planning/phases/01-visible-art-in-werkstattgasse/01-RESEARCH.md (Planner Revision Instructions and memory/handoff contract), .planning/phases/01-visible-art-in-werkstattgasse/01-PATTERNS.md (Kerni embodiment and explicit-user-authority patterns)</read_first>
  <action>
Keep Kerni recognizably the intro raccoon in familiar form and the first helper, companion, and Schaffenwerkzeug per D-08, but preserve the application-owned authority boundary. Keep the existing small familiar always physically present and peripheral beside the first Z1 bench per D-09; presence may improve bench/Kerni legibility but cannot spawn, unlock, or authorize Kerni. Preserve a clear walking path, add no blocking collider, and do not make Kerni follow the player.

At the UI-approved 2.6m range, present a bottom-center keyboard/click control at least 44px high with keycap `E`, `Listen to Kerni`, and the exact inspect-only label `KERNI · WORLD AGENT · SUGGESTION ONLY` per D-10 and D-12. Hide the label outside proximity/inspect instead of making it a permanent nameplate. Route `E` only when proximity is true, the walk/world control owns focus, and no feed/dialog/control/modal owns focus. Clicking the control follows the same intent path. Open a focus-contained soot dialogue with subtitle semantics, and close it with `Escape`, a second `E` only while the dialogue owns that interaction, or `Close Kerni dialogue`; return focus to the safe world interaction control without leaking the closing keystroke into scene actions.

On the first accepted explicit interaction, show exactly this single D-11 line and no other spoken orientation: `Welcome. No rush — the Palace gets better when people leave something useful behind. Start with one small thing.` Keep opening/showing the line separate from acknowledgement. Only the player's explicit acknowledgement, accepted by the reducer after that explicit interaction and the locked line, records one bounded application-owned `RelayMemoryFragment` fact with concrete semantics `{ source: "kerni-orientation", meaning: "leave-one-small-useful-thing" }`; make it idempotent and phase-run scoped, with no second spoken line and no broad persistence. After acknowledgement, keep Kerni quiet on subsequent interaction rather than repeating or expanding a quest rail. Kerni may point/tilt toward the bench only as suggestion-only presentation.

At the workbench, present a separate explicit player choice labeled `Begin relay` whose intent value is `connect-with-others`. Accept that user action only when the memory fragment already exists, and record it as a bounded `RelayInspirationChoice`; opening the workbench, entering proximity, or rendering the control cannot choose it. Derive a pure `relayAssemblyEligible` selector only from both accepted facts for Plan 03; do not introduce assembly actions in this plan. This accepted memory plus accepted inspiration is the complete cross-plan handoff to Plan 03, not assembly itself. Reducer actions must reject attempts by presence acceptance, Kerni, AI/world-agent output, proximity, dialogue open/close, animation completion, scene callbacks, presentation props, or synthetic non-user origins to create either fact, enable assembly, choose, generate, assemble, place, sign, publish, activate, or complete the relay per D-13. Extend the smoke with positive/idempotent tests for acknowledgement → `RelayMemoryFragment` → explicit `connect-with-others` choice → `RelayInspirationChoice` → `relayAssemblyEligible === true`, plus negatives proving presence alone, proximity, dialogue open, animation, Kerni, and scene callbacks create neither memory nor inspiration and leave `relayAssemblyEligible === false`.

Accept only a boolean/epoch-style presentation prop derived from the Plan-01 reducer's accepted placement fact for D-14. A newly accepted placement may cause one nonverbal 10° lens tilt and one 600ms copper-ring settle; reduced effects uses one static lens-brightness change for 1.2s. Baseline mount, rejected/pending placement, rerender, feed/presence changes, and repeated accepted-state renders do not replay it. Kerni emits no action from this response, speaks no second line, and grants no truth. Extend the smoke with reducer negatives and source contracts for permanent presence, fixed peripheral positioning, 2.6m proximity, world-focus ownership, exact label/line, one-shot accepted-only nonverbal presentation, subtitles, and reduced effects.
  </action>
  <verify>
    <automated>pnpm --filter @600b/web exec tsx tests/phase1-presence-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
- D-08 and D-09 are source-tested: Kerni is the intro raccoon/helper in small, always-present peripheral Z1 presentation, without spawning, unlocking, following, or blocking.
- D-10 is proven by proximity plus explicit `E`/click and by negative focus tests showing feed/dialog/buttons/modals retain keyboard authority.
- D-11 is exact-string tested as the only spoken orientation line and remains quiet after acknowledgement.
- D-12 is exact-string tested as a proximity/inspect-only label: `KERNI · WORLD AGENT · SUGGESTION ONLY`.
- D-13 reducer negatives prove Kerni and presentation effects have no choose/generate/assemble/place/sign/publish/activate/complete transition authority.
- D-14 presentation is accepted-placement-only, nonverbal, one-shot, subtitle/reduced-effects compatible, and cannot dispatch state.
- ART-01 tests prove only explicit player acknowledgement after the accepted D-11 interaction creates the single bounded `RelayMemoryFragment` with source `kerni-orientation` and meaning `leave-one-small-useful-thing`.
- ART-01 tests prove only the later explicit workbench `Begin relay` / `connect-with-others` player action combines with that fragment to create `RelayInspirationChoice` and make `relayAssemblyEligible` true; presence, proximity, dialogue-open, animation, Kerni, scene, and presentation callbacks leave both facts absent and the selector false.
  </acceptance_criteria>
  <done>Kerni remains a peripheral, focus-safe helper with the locked label and one locked line; explicit acknowledgement creates one bounded application-owned memory fragment, a separate explicit workbench choice creates inspiration, and those accepted facts—never Kerni or presentation—form the tested ART-01 handoff to Plan 03 while only later accepted placement can trigger the nonverbal acknowledgement.</done>
  <reversibility rating="reversible">Kerni receives presentation-only props and phase-scoped UI/CSS; no model asset, signer, transport, package, or persistent schema is changed.</reversibility>
</task>

</tasks>

<threat_model>
## ASVS-L1 scope and trust boundaries

This is an ASVS-L1 client-side control checklist, not a certification claim. Any critical or high finding blocks completion until its mitigation is implemented and the focused smoke is green.

| Boundary | Untrusted input / authority risk | Enforced owner |
|---|---|---|
| Browser visibility, focus, and elapsed samples → `AttentivePresenceState` | Forged or suspended elapsed time could fabricate ART-01 presence. | Pure `phase1Relay` reducer validates mode and bounded deltas; `PalaceScene` resets sampling baselines on hide/blur/reopen. |
| DOM overlay focus → world keyboard controls | `E`, Escape, or movement could leak through and dismiss, interact, or advance two surfaces. | `Phase1RelayOverlay` contains/returns focus; `PalaceScene` ignores world controls while DOM/modal controls own focus. |
| Reducer facts → light/audio/subtitle/animation/Kerni props | Presentation or Kerni could be mistaken for transition authority. | Reducer owns acceptance; scene/UI/Kerni consume read-only accepted facts and emit only explicit user intent. |
| Explicit dialogue acknowledgement and workbench choice → `RelayMemoryFragment` / `RelayInspirationChoice` | Presence, proximity, open dialogue, Kerni, scene, or presentation callbacks could fabricate the ART-01 semantic bridge. | The reducer requires ordered, focus-safe player intents, records one bounded fragment and one bounded choice, and exposes no presentation-origin transition. |
| Local canonical copy → rendered feed/dialog | A remote or duplicated source could introduce real-feed content, unsafe markup, or conflicting authority copy. | `onboardingStory` plus fixed typed local data are canonical; React text rendering only, with no network/HTML ingestion. |

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-02-01 | Tampering / Elevation | `AttentivePresenceState` elapsed actions | high | mitigate | Reject non-finite/negative deltas, cap accepted active-frame samples at 250ms, require foreground focus plus Wire-away state, reset sample baseline across blur/hide/reopen, and test 35,999/36,000ms plus suspension gaps before completion. |
| T-01-02-02 | Spoofing / Elevation | Kerni, memory/inspiration handoff, and accepted-state presentation | high | mitigate | Keep Kerni/light/audio/subtitle/scene callbacks outside reducer authority; require explicit ordered player acknowledgement and workbench choice for the bounded `RelayMemoryFragment` and `RelayInspirationChoice`; test that presence, proximity, dialogue open, Kerni, animation, DOM, scene, and presentation events cannot create those facts, enable assembly, or accept relay state. |
| T-01-02-03 | Tampering | DOM focus → world `E`/Escape/movement | medium | mitigate | Contain overlay/dialog focus, suppress scene keys whenever interactive DOM or modal focus is active, release held movement on focus entry, and return focus without replaying the closing key. |
| T-01-02-04 | Denial of service | visibility suspension and oversized resume deltas | medium | mitigate | Use monotonic active-frame sampling, discard hidden/blurred gaps, bound each reducer sample, and keep accepted state monotonic so repeated reopen/suspension cannot erase progress. |
| T-01-02-05 | Spoofing / Information disclosure | Wire content rendering | medium | mitigate | Use exactly three local fictional text records, no fetch or user/relay input, no unsafe HTML, no avatars/identity claims, and source-test the fixed copy and lack of remote dependency. |
| T-01-02-06 | Repudiation | one-time Kerni dialogue/reaction | low | accept | The bounded client phase keeps only in-run acknowledged/reaction state; no durable audit claim is made, and broad persistence is explicitly outside this plan. |
</threat_model>

<verification>
1. Run `pnpm --filter @600b/web exec tsx tests/phase1-presence-smoke.ts` after each task; the file must cover canonical intro/feed, 36-second threshold boundaries, reopen/suspension monotonicity, focus ownership, accessibility, D-08–D-14 Kerni contracts, and the ordered explicit acknowledgement → `RelayMemoryFragment` → workbench choice → `RelayInspirationChoice` handoff with authority negatives.
2. Run `pnpm --filter @600b/web exec tsx tests/phase1-tracer-smoke.ts` to ensure the accepted Plan-01 relay path remains intact and presentation changes have not gained authority.
3. Run `pnpm --filter @600b/web typecheck` after the plan wave.
4. Inspect the rendered desktop path at 1280×720 and 200% zoom with keyboard only, then repeat muted and with reduced effects: all Wire cards/actions fit or internally scroll, subtitles and static cues remain legible, focus is contained/returned, and no elapsed/progress/objective UI appears.
5. Treat any unresolved critical/high threat, any D-01–D-14 coverage failure, or any ART-01 interruption/suspension failure as blocking.
</verification>

<success_criteria>
- All three tasks and the focused smoke pass, and D-01 through D-14 each have an executable assertion or source contract.
- The intro order and canonical cards remain authoritative; the freely walkable glimpse lasts 2400ms; the exact local Wire can always be put away without engagement and optionally reopened.
- Presence accepts at exactly 36 seconds of valid foreground world attention, never gains time from feed-open/hidden/suspended gaps, never resets, and remains accepted monotonically.
- Populated and overflow UI states work at 1280×720 and 200% zoom with keyboard/focus containment, subtitles, mute, reduced effects, non-color cues, and polite terminal status.
- Kerni remains peripheral and suggestion-only, exposes the exact label and one exact line only after explicit interaction, and has no authority beyond one accepted-placement-driven nonverbal presentation.
- ART-01's encounter handoff is explicit: acknowledgement of the one accepted D-11 line records one bounded `kerni-orientation` / `leave-one-small-useful-thing` memory fragment, then the player's separate `Begin relay` / `connect-with-others` workbench choice records inspiration for Plan 03; no passive or scene-owned path can create either fact or enable assembly.
- No prohibited feed, engagement, timer/progress/objective, quest, fake-peer, Kerni-authority, package, schema, generic-builder, or direct-SDK scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-visible-art-in-werkstattgasse/01-02-SUMMARY.md` when execution is complete.
</output>
