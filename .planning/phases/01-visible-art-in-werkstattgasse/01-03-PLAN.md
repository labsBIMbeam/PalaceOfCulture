---
phase: 01-visible-art-in-werkstattgasse
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - apps/web/src/meaningverse/phase1Relay.ts
  - apps/web/src/ui/Phase1RelayOverlay.tsx
  - apps/web/src/scene/PalaceScene.tsx
  - apps/web/src/scene/StreetWorld.tsx
  - apps/web/src/scene/KerniFamiliar.tsx
  - apps/web/tests/phase1-placement-smoke.ts
autonomous: true
napplet_boundary:
  classification: native-world
  participating_napplets: []
  presentation: [diegetic-world, focused-overlay]
  registry_effect: none
  reason: "The under-ten-minute loop is continuous movement, spatial assembly, placement, lighting, and in-world feedback inside the existing Palace runtime."
  rejected_alternative: "A separate Napplet or browser-looking window would fragment embodied play and duplicate the existing application-truth and transport seams."
requirements:
  - ART-01
  - ART-02
  - PLAC-05
must_haves:
  truths:
    - "ART-01 advances from the acknowledged `kerni-orientation` / `leave-one-small-useful-thing` memory fragment plus the player's explicit `connect-with-others` / `Begin relay` inspiration choice into the handmade luminous relay, with assembly entry eligible only when both accepted facts are present."
    - "A player completes the handmade relay only by physically picking up and seating exactly three visible parts in order: foot, then coil, then aperture (D-17)."
    - "Exactly one fixed socket at Kerni's Z1 workbench accepts the completed relay, with a single idempotent winner under duplicate or concurrent placement intent (D-18, PLAC-05)."
    - "Interruption, failure, cancellation, stale completion, and retry preserve the previously accepted world truth; only a matching reducer acceptance can create the one accepted placement."
    - "Only accepted placement produces the final snap, persistent amber light, restrained OPEN status, transport eligibility, invite eligibility, and Kerni's one nonverbal response (D-14, D-19, D-20)."
    - "Partial assembly and carried state remain physically legible, while invalid, pending, and failed placement clearly communicate that the world has not changed."
  artifacts:
    - path: apps/web/src/meaningverse/phase1Relay.ts
      provides: "RelayMemoryFragment and RelayInspirationChoice assembly preconditions, derived relayAssemblyEligible handoff, RelayPart, AssemblyState, PlacementState, ordered assembly transitions, placement validator, single-winner acceptance reducer, and permanent legacy direct-activation no-op"
    - path: apps/web/src/scene/StreetWorld.tsx
      provides: "RelayWorkbench with three cradles, three visible parts, one fixed Z1 socket, accepted relay, and RelayStatusPlate"
    - path: apps/web/src/ui/Phase1RelayOverlay.tsx
      provides: "Keyboard-accessible pickup/place controls and partial, invalid, pending, failed, retry, and accepted status output"
    - path: apps/web/tests/phase1-placement-smoke.ts
      provides: "Executable assembly, fit, authority, interruption, failure, retry, duplicate, race, and presentation contracts"
  key_links:
    - from: apps/web/src/scene/PalaceScene.tsx
      to: apps/web/src/meaningverse/phase1Relay.ts
      via: "proximity/focus-safe pickup, seating, placement, and attempt-correlated completion intents are reduced before any scene effect; the legacy generic direct-activation path is absent/no-op"
      pattern: "reducePhase1Relay"
    - from: apps/web/src/meaningverse/phase1Relay.ts
      to: apps/web/src/scene/StreetWorld.tsx
      via: "accepted application facts drive RelayWorkbench and fixed-socket presentation props"
      pattern: "assembly|placement"
    - from: apps/web/src/meaningverse/phase1Relay.ts
      to: apps/web/src/ui/Phase1RelayOverlay.tsx
      via: "placement state selects honest accessible status and recovery actions"
      pattern: "idle|valid|invalid|pending|accepted|failed"
    - from: apps/web/src/meaningverse/phase1Relay.ts
      to: apps/web/src/scene/KerniFamiliar.tsx
      via: "the accepted placement edge triggers one presentation-only acknowledgement"
      pattern: "accepted"
  prohibitions:
    - "No text input, generative AI, Kerni action, animation completion, local mesh position, or presentation callback may assemble, place, or accept the relay."
    - "No generic BuilderHud, inventory, catalog, recipe, arbitrary grid, multiple sockets, free placement, or generalized crafting surface."
    - "No terraforming, paid placement, open upload, spam-scale placement, structural construction, broad persistence, economy, or production-scale multiplayer scope."
    - "No direct Nostr/NDK SDK import or effect authority in scene/UI code."
    - "No new packages, package installation, database migration, schema change, or schema push."
---

<objective>
Expand the proven Phase-1 relay path through the explicit ART-01 semantic bridge—accepted memory fragment plus the player's explicit inspiration choice—into a tactile, exactly-three-part handmade luminous relay, transactional placement at one fixed Z1 socket, and activation rendered only from accepted application truth while permanently closing the Plan-01 tracer direct-activation bypass.

Purpose: Deliver ART-01's memory+inspiration → handmade luminous relay bridge and the physical creation and finite-slot world change required by ART-02 and PLAC-05 while preserving Kerni's suggestion-only role (D-14), the useful luminous relay identity (D-16), and the app-owned transport boundary (D-20).
Output: Tested assembly and placement reducers, one bounded Z1 RelayWorkbench, honest partial/error UI, accepted-only amber activation and OPEN, and focused placement smoke coverage.
</objective>

<execution_context>
@~/.hermes/gsd-core/workflows/execute-plan.md
@~/.hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-CONTEXT.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-RESEARCH.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-PATTERNS.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-VALIDATION.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-UI-SPEC.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate and reduce exactly-three-part physical relay assembly from memory plus inspiration</name>
  <files>apps/web/src/meaningverse/phase1Relay.ts, apps/web/tests/phase1-placement-smoke.ts</files>
  <read_first>
    - `.planning/phases/01-visible-art-in-werkstattgasse/01-CONTEXT.md` D-13, D-16, and D-17 for the handmade relay and authority boundary.
    - `.planning/phases/01-visible-art-in-werkstattgasse/01-UI-SPEC.md` sections 5, Application Truth vs Presentation State, State Matrix, and Accessibility Contract for exact part order and physical interaction.
    - `.planning/phases/01-visible-art-in-werkstattgasse/01-RESEARCH.md` Planner Revision Instructions, Open Questions (RESOLVED), and memory/handoff contract for the ART-01 semantic bridge and tracer-bypass closure.
    - `.planning/phases/01-visible-art-in-werkstattgasse/01-PATTERNS.md` sections App truth separate from effects, Bounded Z1 world composition, and Exact smoke-test style.
    - `apps/web/src/meaningverse/phase1Relay.ts` for the reducer/action contracts established by Plans 01-02.
    - `apps/web/tests/phase1-tracer-smoke.ts` and `apps/web/tests/phase1-presence-smoke.ts` for the existing direct `node:assert/strict` smoke style and accepted-state invariants.
  </read_first>
  <behavior>
    - RED: absent memory, absent inspiration, memory alone, or inspiration-like scene/Kerni/presentation callbacks leave assembly ineligible and a physical `foot` pickup unchanged; only the accepted `RelayMemoryFragment` plus accepted explicit player `RelayInspirationChoice` opens assembly entry.
    - RED: zero seated parts plus a physical pickup intent for `foot` produces a carried foot; seating it in the foot cradle produces exactly one seated part.
    - RED: the only completion sequence is `foot → coil → aperture`; each accepted seating leaves the corresponding part physically represented and makes only the next part eligible.
    - RED: wrong-part pickup, wrong cradle, out-of-order seating, repeated pickup/seating, and seating without carrying leave accepted assembly facts unchanged and retain a recoverable carried state where applicable.
    - RED: intents labeled as text, AI/world-agent, Kerni, scene callback, animation, mesh-position, or presentation origin cannot create memory/inspiration, enable assembly, create a pickup, seated part, carried relay, or completed assembly (D-13, D-17, D-20).
    - RED: after the aperture is seated, exactly three accepted parts become one carried completed relay; no fourth part, recipe, inventory item, or generic construction state exists.
  </behavior>
  <action>
Write the failing assembly assertions first in `phase1-placement-smoke.ts`, run the focused smoke and confirm RED, then extend `RelayPart`, `AssemblyState`, `Phase1RelayAction`, and `reducePhase1Relay` in the existing pure truth module. Consume the Plan-02 `RelayMemoryFragment`, `RelayInspirationChoice`, and derived `relayAssemblyEligible` contract directly, while rechecking both underlying accepted facts inside every reducer mutation: assembly entry and the first `foot` pickup require the accepted fragment `{ source: "kerni-orientation", meaning: "leave-one-small-useful-thing" }` plus the accepted explicit player choice `connect-with-others`; neither alone nor any synthesized scene/Kerni/presentation callback is sufficient. This memory+inspiration → handmade luminous relay transition is the explicit ART-01 semantic bridge. Represent the three parts as a closed union in the exact D-17/UI-SPEC order `foot`, `coil`, `aperture`; model physical pickup, current carry, matching-cradle seating, accepted seated order, and the transition to one carried completed relay. Require an explicit trusted local world-interaction origin plus current reducer eligibility for every mutation. Wrong cradle/order and repeated or nonphysical intent are resistant no-ops or typed rejections that do not erase prior accepted parts. Keep all geometry, animation, text, Kerni, and effects outside the transition authority. Do not add a parallel store or generalized crafting abstraction: this is the D-16 luminous relay's bounded assembly only. Run GREEN, then refactor without broadening the action surface.
  </action>
  <verify>
    <automated>pnpm --filter @600b/web exec tsx tests/phase1-placement-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove exactly three parts and only the order foot → coil → aperture can produce the carried completed relay (D-17).
    - Tests prove the first foot pickup and every assembly mutation require both the accepted Plan-02 memory fragment and explicit player inspiration choice; either fact alone and every Kerni/scene/presentation substitute is a no-op.
    - Tests prove every partial state from 0 through 3 seated parts plus carry state is explicit and monotonic.
    - Tests prove wrong cradle/order, duplicates, and nonphysical or unauthorized origins cannot advance or erase assembly truth (D-13, D-20).
    - The reducer stays pure, closed over Phase-1 types, and introduces no generic inventory, recipe, catalog, or crafting engine.
  </acceptance_criteria>
  <done>The reducer crosses the explicit ART-01 memory+inspiration bridge before accepting the three ordered physical actions, preserves recoverable partial state on resistance, and emits one completed carried handmade luminous relay with the focused smoke green.</done>
  <reversibility rating="reversible">The bounded union and pure transitions can be changed behind the existing Phase-1 reducer without data migration or external side effects.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce transactional single-winner placement at the fixed Z1 socket</name>
  <files>apps/web/src/meaningverse/phase1Relay.ts, apps/web/tests/phase1-placement-smoke.ts</files>
  <read_first>
    - `.planning/phases/01-visible-art-in-werkstattgasse/01-PLAN-OUTLINE.md` section `01-03 — Physical Relay Assembly and Fixed-Socket Placement`, especially the PLAC-05 concurrency probe.
    - `.planning/phases/01-visible-art-in-werkstattgasse/01-CONTEXT.md` D-18 through D-20 for one socket, accepted light, and app-owned truth.
    - `.planning/phases/01-visible-art-in-werkstattgasse/01-UI-SPEC.md` section 5 and required placement vocabulary `idle | valid | invalid | pending | accepted | failed`.
    - `.planning/phases/01-visible-art-in-werkstattgasse/01-RESEARCH.md` Pattern 2 and Pitfall 3 for accepted activation and fail-closed visible state.
    - `apps/web/src/meaningverse/phase1Relay.ts` and the Task-1 tests for the completed-carried-relay precondition and existing accepted activation seam.
  </read_first>
  <behavior>
    - RED: only a completed carried relay at the sole fixed socket ID `z1-relay-socket` can move preview from idle to valid and submit one pending placement attempt (D-18).
    - RED: any other target or incomplete relay yields invalid while the player keeps the relay and accepted world truth remains unchanged.
    - RED: the first valid request records one opaque attempt ID as the pending winner; duplicate same-ID requests are idempotent and concurrent different-ID requests cannot replace it, consume another slot, or create another artifact.
    - RED: acceptance succeeds only for the current pending attempt; it atomically records one accepted socket placement and one activation edge. Duplicate, stale, late, or losing acceptances are no-ops and do not increment an epoch or replay activation.
    - RED: interruption/cancellation or failure of the current pending attempt moves to a recoverable failed state while retaining the completed relay; stale failure cannot overwrite accepted truth.
    - RED: retry from failed creates a new pending attempt but preserves the prior world truth until that exact attempt is accepted; failure/retry never lights the relay or exposes OPEN.
    - RED: once accepted, every later preview, place, retry, fail, or accept intent is idempotent and the sole accepted winner remains unchanged.
    - RED: the Plan-01 legacy generic scene direct-activation action/path, including a forged `pending → accepted` dispatch, is a permanent no-op and produces no accepted placement, activation edge, `relayOpen`, light, transport eligibility, or invite eligibility.
  </behavior>
  <action>
Write the interleaving, legacy-bypass, and negative tests first and observe RED. Extend `PlacementState` as the exact UI-SPEC state vocabulary and add a phase-local validator plus attempt-correlated reducer actions. Remove the Plan-01 generic scene `pending → accepted` shortcut from the production `Phase1RelayAction` vocabulary and from dispatch call sites; if a compatibility parser or regression fixture can still submit that legacy shape, handle it as a permanent no-op. The only acceptance action must verify, in the reducer at transition time, the accepted memory fragment, accepted explicit inspiration choice, completed `foot → coil → aperture` assembly, current fixed socket ID `z1-relay-socket`, and exact current pending attempt winner. Use one fixed socket identity and one accepted placement record; never derive validity from arbitrary mesh coordinates. Resolve PLAC-05 concurrency with compare-current-attempt semantics: the first eligible request owns pending, exact duplicates return equivalent state, contenders are rejected without replacement, and only a matching completion can atomically accept. Cancellation/failure returns the same completed relay to recoverable control and records honest failure without accepted world mutation. A retry gets a fresh attempt ID but cannot erase or fabricate prior truth. After acceptance, freeze the winner and activation edge so duplicates, stale callbacks, effect completion, local mesh state, and the old direct tracer path cannot create extra objects, slots, accepted/open/light state, or transport eligibility (D-18, D-19, D-20). Keep state bounded to the Phase-1 handoff; do not add persistence or schema work. Run GREEN and refactor repeated guard logic while preserving fail-closed ordering.
  </action>
  <verify>
    <automated>pnpm --filter @600b/web exec tsx tests/phase1-placement-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    - The exact placement vocabulary exists and only `z1-relay-socket` can validate for the completed relay (D-18, PLAC-05).
    - Race tests prove one accepted winner, same-intent idempotency, different-intent exclusion, and no duplicate artifact, slot consumption, activation edge, or epoch change.
    - Interruption, failure, stale completion, and retry tests prove the accepted world fact remains exactly what it was before each operation.
    - Persistent light, OPEN, communication eligibility, invite eligibility, and Kerni acknowledgement are derivable only from `placement === accepted` (D-14, D-19, D-20).
    - A regression fixture exercises the exact old generic scene direct-activation action/path and proves no accepted placement, activation edge, OPEN/light, transport, invite, or Kerni state, even from a forged pending state; only the exact current fixed-socket winner with memory, inspiration, and completed ordered assembly can be accepted.
  </acceptance_criteria>
  <done>The fixed-socket reducer is single-winner and idempotent under all tested interleavings; failure and retry remain recoverable, and the legacy direct tracer action is permanently absent/no-op while only a matching fully gated acceptance can change world truth.</done>
  <reversibility rating="reversible">Attempt correlation and the bounded session record are pure in-memory Phase-1 state with no schema or persistence migration.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Present the bounded Z1 workbench and accepted-only activation</name>
  <files>apps/web/src/ui/Phase1RelayOverlay.tsx, apps/web/src/scene/PalaceScene.tsx, apps/web/src/scene/StreetWorld.tsx, apps/web/src/scene/KerniFamiliar.tsx, apps/web/tests/phase1-placement-smoke.ts</files>
  <read_first>
    - `.planning/phases/01-visible-art-in-werkstattgasse/01-UI-SPEC.md` sections 5, Application Truth vs Presentation State, State Matrix, Responsive and Desktop Runtime Contract, Animation and Timing Contract, and Accessibility Contract.
    - `.planning/phases/01-visible-art-in-werkstattgasse/01-PATTERNS.md` sections `PalaceScene.tsx` as orchestration only, Bounded Z1 world composition, Accepted-state rendering, and Transport and scene isolation.
    - `.planning/phases/01-visible-art-in-werkstattgasse/01-CONTEXT.md` D-14 and D-16 through D-20.
    - `apps/web/src/scene/StreetWorld.tsx` for the explicit Workshop/Kerni composition and fixed Z1 topology.
    - `apps/web/src/scene/PalaceScene.tsx` for current proximity, `E`, focus ownership, reducer orchestration, and accepted-effect gates.
    - `apps/web/tests/phase1-tracer-smoke.ts` for the legacy direct scene activation action/path that this task must remove from production wiring and preserve only as a no-op regression fixture.
    - `apps/web/src/scene/KerniFamiliar.tsx` for its existing small nonblocking familiar and non-shadow warm-light pattern.
    - `apps/web/src/ui/Phase1RelayOverlay.tsx` for the Plan-02 accessible status/focus conventions.
  </read_first>
  <action>
Add one explicit `RelayWorkbench` group to the existing Z1 workshop composition near Kerni without blocking the clear center lane. Render exactly the copper foot, ceramic coil, and amber aperture as distinct visible silhouettes, their three matching cradles, the carried/0–3 seated partial state, one brass-collared fixed socket, and a small `RelayStatusPlate`. Show pickup/assembly controls only from reducer eligibility after accepted memory plus explicit inspiration. Route proximity plus focus-safe `E`/44px on-screen actions through `PalaceScene` strictly as pickup, seating, placement-request, failure, retry, or attempt-correlated completion intents to the reducer; remove the old generic direct-activation dispatch/callback. Scene mesh position and animation callbacks are never acceptance, and only the reducer's matching placement acceptance owns truth. Present `VALID` with teal etched/check-notch feedback and `E · Place relay`; present invalid resistance with coral crossed seam and `This relay fits the workbench socket.` while keeping the relay carried. Present pending as an unlit seated relay with `Securing relay…`; after the UI-SPEC eight-second bound route a failure intent, not acceptance, and show `The relay did not secure. Try the socket again.` with `Try again` and `Keep holding`. Ensure the partial UI always shows the actual 0–3 seated pieces/current carry and omits unsupported attribution. Only reducer-accepted placement performs the final 4cm snap, enables a static-readable amber core plus at most one non-shadow 1.4m warm light, exposes restrained `OPEN`, and triggers Kerni's single 10°/600ms nonverbal response (static 1.2s lens change under reduced motion) per D-14 and D-19. Keep the light legible with bloom off and make terminal acceptance/failure announce once through `aria-live="polite"` with text plus shape/icon. Add a small noninteractive, truthful indication that richer Palace placement requires earned furnishing rights, satisfying PLAC-05 without implementing those rights. Preserve D-20 by passing only app-facing facts/actions; scene and UI remain Nostr/NDK-free. Extend the smoke with source/wiring contracts for one workbench/socket, three ordered parts, focus-safe input, partial/error copy, accepted-only light/OPEN/Kerni reaction, non-shadow/mobile-budget lighting, and the absence of alternate placement entry points.
  </action>
  <verify>
    <automated>pnpm --filter @600b/web exec tsx tests/phase1-placement-smoke.ts &amp;&amp; pnpm --filter @600b/web typecheck</automated>
    <human-check>At 1280×720, inspect Z1 with keyboard only and with reduced effects/bloom off: the center lane stays clear; all three parts and partial states are visible; invalid, pending, failure, and retry never light the relay; one accepted placement yields one amber relay, OPEN, and one silent Kerni acknowledgement.</human-check>
  </verify>
  <acceptance_criteria>
    - Z1 contains exactly one bounded workbench group, three visible parts/cradles in foot → coil → aperture order, and exactly one fixed socket (D-17, D-18).
    - Partial state and carried state are visible; invalid/pending/failed status uses the exact honest copy and never claims acceptance.
    - Accepted placement alone produces the final snap, one non-shadow amber light, OPEN, transport/invite eligibility, and one nonverbal Kerni reaction (D-14, D-19).
    - PalaceScene has no generic direct-activation dispatch: it emits only bounded intents, assembly controls remain unavailable before accepted memory plus explicit inspiration, and the old tracer action/path is source- and reducer-tested as a no-op with no accepted/open/light/transport state.
    - Keyboard/on-screen actions, focus ownership, non-color status, aria-live terminal output, reduced effects, 1280×720 clearance, and mobile 30 FPS constraints are represented and tested.
    - Higher-tier capability is described honestly without adding a catalog, grid, free placement, persistence, package, or schema path (PLAC-05, D-20).
  </acceptance_criteria>
  <done>The Z1 workbench visibly supports the memory+inspiration-gated physical assembly and one fail-closed socket flow; PalaceScene emits intents only, the old direct shortcut is absent/no-op, and only the reducer's matching accepted placement fact changes the world to one amber OPEN relay.</done>
  <reversibility rating="reversible">The workbench is an explicit scene child and all effects are derived from reversible Phase-1 props rather than asset, schema, or topology changes.</reversibility>
</task>

</tasks>

<threat_model>
## ASVS-L1 Trust Boundaries

| Boundary | Untrusted input or authority risk | Required control |
|---|---|---|
| World/DOM interaction → pure reducer | Repeated, synthesized, stale, or focus-leaked pickup/place intent | Closed action/source unions, eligibility checks, focus ownership, and idempotent reducer transitions before effects |
| Plan-01 legacy generic direct activation → final reducer/scene wiring | A stale production action or callback could bypass memory, inspiration, ordered assembly, and fixed-socket winner correlation. | Remove it from production action vocabulary and PalaceScene dispatch; retain an adversarial fixture whose reducer result is a permanent no-op with no accepted/open/light/transport truth. |
| Placement request → asynchronous completion | Duplicate/concurrent request, late success, cancellation/failure race, or retry confusion | One opaque pending attempt winner; exact-attempt compare before acceptance; accepted winner freezes later transitions |
| Application fact → local Three.js/DOM presentation | Mesh position, snap completion, timer, light, overlay, or Kerni reaction attempts to authorize truth | One-way accepted facts as props; effects emit no acceptance and cannot expose OPEN or transport eligibility |
| Repeated input → browser work/effects | Key repeat or intent flood causes allocation, animation, or denial of interaction | Reducer no-ops in ineligible/duplicate states; one pending attempt/effect; bounded fixed parts/socket; no per-intent scene allocation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-03-01 | Tampering | `reducePhase1Relay` assembly actions | high | mitigate | Require trusted local physical-interaction origin plus exact current eligibility; tests reject text, AI, Kerni, animation, mesh, presentation, wrong-order, and duplicate intents before any accepted part changes. |
| T-01-03-08 | Elevation of privilege | Plan-01 direct tracer activation path | high | mitigate | Remove the generic scene `pending → accepted` action and dispatch from production; reducer compatibility handling is a permanent no-op, with regression fixtures proving no accepted placement, activation edge, OPEN/light, transport/invite eligibility, or Kerni acknowledgement. |
| T-01-03-02 | Tampering / Elevation of privilege | fixed-socket acceptance | high | mitigate | Correlate one pending winner by opaque attempt ID and atomically accept only that current attempt; losing, stale, duplicate, and post-accept completions are no-ops in race tests. |
| T-01-03-03 | Elevation of privilege | `StreetWorld`, `PalaceScene`, overlay, Kerni | high | mitigate | Keep reducer authority in `phase1Relay.ts`; components receive facts and emit intents only; light, snap, timer, mesh position, local effects, and Kerni callbacks cannot dispatch acceptance. |
| T-01-03-04 | Denial of service | repeated keyboard/pointer placement intent | high | mitigate | Collapse repeats through state/attempt idempotency, permit only one pending attempt and one fixed socket, avoid per-intent object/effect allocation, and exercise bounded repeat bursts in the focused smoke. |
| T-01-03-05 | Spoofing | visible OPEN/light state | high | mitigate | Derive persistent amber light, OPEN, communication/invite eligibility, and Kerni response exclusively from accepted reducer truth; pending, failure, retry, local mesh, and callback states render unlit/unchanged. |
| T-01-03-06 | Repudiation | interrupted/failed/retried placement | medium | mitigate | Preserve explicit attempt ID and terminal placement state in bounded reducer truth so tests can prove prior accepted world truth remains unchanged across cancellation, failure, stale completion, and retry. |
| T-01-03-07 | Information disclosure | placement status UI | low | accept | Status contains only fixed local Phase-1 part/socket labels and no secrets, signer data, event content, or user-supplied HTML. |

All critical/high threats are completion-blocking until their listed automated tests pass; no high threat is accepted or transferred.
</threat_model>

## Artifacts this phase produces

- `apps/web/src/meaningverse/phase1Relay.ts` — tested `RelayMemoryFragment` + `RelayInspirationChoice` assembly gate, ordered assembly, transactional fixed-socket truth, and permanent legacy direct-activation no-op.
- `apps/web/src/scene/StreetWorld.tsx` — one bounded `RelayWorkbench`, three visible parts/cradles, one fixed socket, and accepted relay presentation.
- `apps/web/src/scene/PalaceScene.tsx` — focus-safe intent-only routing with the old generic direct-activation dispatch removed and accepted-only effect orchestration.
- `apps/web/src/ui/Phase1RelayOverlay.tsx` — partial, fit, pending, failed/retry, accepted, and accessible status output.
- `apps/web/src/scene/KerniFamiliar.tsx` — one presentation-only nonverbal acknowledgement from accepted placement.
- `apps/web/tests/phase1-placement-smoke.ts` — memory/inspiration gate, assembly, placement, concurrency, legacy direct-activation no-op, authority, UI, and wiring evidence.

<verification>
1. Run `pnpm --filter @600b/web exec tsx tests/phase1-placement-smoke.ts` after each task; it must prove the accepted memory+explicit inspiration assembly gate, ordered physical assembly, single-socket validation, one accepted winner, duplicate idempotency, interruption/failure/retry invariants, repeated-input bounds, the Plan-01 legacy direct-activation no-op with no accepted/open/light/transport state, and accepted-only presentation.
2. Run `pnpm --filter @600b/web exec tsx tests/phase1-tracer-smoke.ts` and `pnpm --filter @600b/web exec tsx tests/phase1-presence-smoke.ts` to ensure the accepted relay seam and Kerni/presence authority boundaries remain intact.
3. Run `pnpm --filter @600b/web typecheck` and `pnpm --filter @600b/web test` at wave completion.
4. Inspect at 1280×720, keyboard-only, bloom off, and reduced effects: verify clear center lane, legible 0–3 partial/carry state, exact fit/error/retry states, no light before acceptance, and one amber OPEN relay plus one silent Kerni reaction after acceptance.
5. Treat any unresolved critical/high threat, any assembly path lacking accepted memory plus explicit player inspiration, or any path from the legacy tracer action, scene, mesh, effect, Kerni, text, AI, or repeated input directly to accepted truth as plan failure.
</verification>

<success_criteria>
- ART-01 and D-17 are complete: the accepted `kerni-orientation` / `leave-one-small-useful-thing` memory fragment plus the player's explicit `connect-with-others` / `Begin relay` inspiration choice are required before exactly three visible physical parts assemble in foot → coil → aperture order without text, AI, Kerni, scene, or presentation authority.
- D-18 and PLAC-05 are complete for this bounded slice: exactly one fixed Z1 socket validates placement, with visible legal preview/rejection and truthful higher-tier unavailability.
- The PLAC-05 concurrency edge is closed: one winner is accepted; duplicate/simultaneous intents are idempotent; cancellation, failure, stale completion, and retry leave prior world truth unchanged.
- D-19 is complete: accepted placement alone snaps and lights the relay as the primary visible world change and exposes restrained OPEN.
- D-14, D-16, and D-20 remain true: Kerni reacts once without authority, the artifact remains the useful handmade luminous relay, and application truth—not local mesh, effects, or transport—owns activation.
- The Plan-01 direct tracer activation shortcut is permanently closed: production PalaceScene cannot dispatch it, compatibility handling is a no-op, and its regression fixture produces no accepted placement, activation edge, OPEN/light, transport/invite eligibility, or Kerni acknowledgement.
- Partial and error UI remain accessible, honest, recoverable, and legible without color, bloom, sound, or strong motion.
- Focused smoke, regressions, full Web tests, and typecheck pass with no new package, schema, generic builder, arbitrary placement, or broad persistence work.
</success_criteria>

<output>
Create `.planning/phases/01-visible-art-in-werkstattgasse/01-03-SUMMARY.md` when execution is complete.
</output>
