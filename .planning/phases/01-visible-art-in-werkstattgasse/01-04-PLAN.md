---
phase: 01-visible-art-in-werkstattgasse
plan: 04
type: execute
wave: 4
depends_on: [01-03]
requirements: [ART-02, PLAC-05]
autonomous: true
napplet_boundary:
  classification: native-world
  participating_napplets: []
  presentation: [diegetic-world, focused-overlay]
  registry_effect: none
  reason: "The under-ten-minute loop is continuous movement, spatial assembly, placement, lighting, and in-world feedback inside the existing Palace runtime."
  rejected_alternative: "A separate Napplet or browser-looking window would fragment embodied play and duplicate the existing application-truth and transport seams."
files_modified:
  - apps/web/src/meaningverse/model.ts
  - apps/web/src/meaningverse/phase1Relay.ts
  - apps/web/src/net/phase1RelayTransport.ts
  - apps/web/src/ui/Phase1RelayOverlay.tsx
  - apps/web/src/scene/PalaceScene.tsx
  - apps/web/tests/phase1-witness-security-smoke.ts
must_haves:
  truths:
    - "Accepted local placement allocates the fixed relay ID `werkstattgasse:z1:relay:1`; route state never creates activation truth, and only a complete, verified, fresh signed activation capability may let a fresh invited browser import bounded lit/bypass/witness eligibility."
    - "After accepted relay activation, the creator sees restrained OPEN status and may explicitly choose Copy invite, consent to `activate-relay-invite`, and approve NIP-07 signing; the activation event is embedded in a sanitized URL but is never published, and display, copy, manual selection, cancellation, or failure never proves sharing."
    - "A fresh browser accepts exactly `join=street` plus the bounded activation capability, validates the exact kind/tags/creator/pubkey/signature and invite age before reducer import, then reaches the same lit relay while bypassing creator intro/feed; malformed, mutated, expired, or incomplete routes take the canonical unlit creator path."
    - "Only explicit eligible E actions plus focused consent can request a NIP-07 signature; activation is sign-only, while witness and lens must receive non-empty-relay publish acknowledgement and then return through a relay-backed live subscription before reducer truth changes."
    - "Only fresh, strictly bounded, correctly signed and dependency-bound kind-9127 activation, witness, and lens evidence can pass the generic transport; unknown, duplicate, reordered, extra, oversized, stale, future, unrelated, invalidly signed, replayed, burst, pre-witness, creator-as-witness, second-witness, or second-lens input changes nothing."
    - "Initial subscription EOSE and reconnect are silent baselines; cache, optimistic, no-relay, duplicate, and late cancelled-attempt callbacks are ignored, and at most one accepted witness plus one accepted lens can produce the bounded pulse/lens presentation."
    - "Verified event pubkeys and IDs add bounded event attribution only and cannot prove humanity, a session handle, account ownership, or broader Palace identity, nor replace, merge, rename, transfer, or reclassify the creator's primary identity or maker attribution."
  artifacts:
    - "apps/web/src/meaningverse/model.ts preserves the sanitized URL builder and supplies bounded activation-invite, consent-attempt, and presentation-state helpers without owning signer, relay, or reducer authority."
    - "apps/web/src/meaningverse/phase1Relay.ts owns fixed-ID local activation, `importVerifiedActivationCapability`, activation/witness/lens dependency authorization, accepted identities, idempotency, silent epoch behavior, and creator-attribution invariants."
    - "apps/web/src/net/phase1RelayTransport.ts owns generic exact kind-9127 templates/parser/validator, raw bounds, installed `verifyEvent`, NIP-07 signing, witness/lens acknowledged publish, relay-backed subscription filtering, freshness/rate/dedup gates, and deterministic fixtures for all three actions."
    - "apps/web/src/ui/Phase1RelayOverlay.tsx renders restrained OPEN invite consent, selectable manual fallback, focused witness/lens consent and truthful pending/failure/status surfaces with retained keyboard, focus, long-text, reduced-motion, and accessibility behavior."
    - "apps/web/src/scene/PalaceScene.tsx orchestrates canonical versus verified invited landing, explicit eligible E actions, cancellable attempt tokens, activation-gated transport lifetime, accepted-delta presentation, and disposal without parsing events or calling SDKs directly."
    - "apps/web/tests/phase1-witness-security-smoke.ts proves network-free invite import, exact three-action contracts, NIP-07 call sequencing, publish acknowledgement, relay-backed receive, bounds, dependency binding, replay/rate/concurrency, baseline, UI, and attribution invariants; `finalizeEvent` is fixture-only."
  key_links:
    - "accepted local placement -> fixed `werkstattgasse:z1:relay:1` -> explicit Copy invite -> capability-detected NIP-07 consent -> verified activation event -> base64url URL -> fresh-browser strict validation -> `importVerifiedActivationCapability`; every link is fail-closed and route state alone has no truth authority."
    - "activation event uses ordered `t/action/relay/creator` tags and is signed but never published; witness uses `t/action/relay/e/p`, lens uses `t/action/relay/e/p/w`, and exact ordering plus activation/creator/witness dependencies are validated before reducer authorization."
    - "eligible E action -> focused consent -> bounded draft -> fresh `NDKNip07Signer(1000, getNdk())` readiness -> `NDKEvent` sign -> `rawEvent()` reparse -> strict shape -> `verifyEvent` -> binding; no arrival, presence, timer, bot, transport callback, or signer success can skip a link."
    - "verified witness/lens event -> `await event.publish(undefined,3000,1,{skipContentTagging:true})` -> non-empty relay set -> relay-backed live subscription callback -> raw bounds -> exact parse -> `verifyEvent` -> binding/freshness/rate/dedup -> reducer; only an accepted reducer delta reaches presentation."
    - "`NDKFilter<number>` scoped by activation ID -> silent EOSE baseline -> relay-backed witness/lens handling -> stop after both accepted or disposal; cache, optimistic publish, no-relay callback, reconnect history, replay, and app-cancelled late results cannot mutate OPEN."
    - "accepted activation, witness, and lens IDs/pubkeys -> bounded secondary attribution, while creator identity and maker attribution remain immutable; Plan 05 consumes accepted lens evidence unchanged."
  prohibitions:
    - "No activation-capability publication, no automatic broadcast/publish/signing, and no claim that a displayed, opened, selected, manually copied, clipboard-copied, or otherwise presented URL was shared or received."
    - "No app-held or demo nsec in production, custom cryptography, invented NDK/nostr-tools API, deep SDK import, new package/dependency, package install, database migration, or schema push; use existing package-root NDK exports and nostr-tools `verifyEvent`."
    - "No unsafe HTML, `dangerouslySetInnerHTML`, event-provided navigation URL, guessed identity, or claim that a valid signature proves a human, session handle, account ownership, or broader Palace identity."
    - "No scene/UI direct SDK use, relay/subscription/signer/publish callback directly setting activation, witness, or lens truth, and no mock, bot, Kerni, timer, count, backlog, ambient message, loopback, presence, cache, optimistic callback, or fake peer as evidence."
    - "No broad chat, voice, multiplayer, social feed, broad persistence, economy, generic placement, generic identity, or remix expansion; Plan 05 alone consumes accepted lens evidence for its scoped behavior."
---

<objective>
Deliver the complete signed activation invite and the generic, fail-closed activation/witness/lens kind-9127 transport for the already accepted luminous relay, including exact NIP-07 signing and acknowledged publish/relay-backed receive (D-20 through D-24).

Purpose: Complete the bounded ART-02/PLAC-05 handoff without turning route state, Nostr, signer success, publish intent, cache, presence, or presentation effects into application truth.
Output: A complete verified activation capability in the sanitized invite, strict generic three-action event transport, explicit signer-consent and acknowledged witness/lens publish/receive orchestration, and focused network-free security regression coverage.
</objective>

<execution_context>
@$HOME/.hermes/gsd-core/workflows/execute-plan.md
@$HOME/.hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-CONTEXT.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-RESEARCH.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-PATTERNS.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-VALIDATION.md
@.planning/phases/01-visible-art-in-werkstattgasse/01-UI-SPEC.md
@CLAUDE.md
</context>

## Artifacts this phase produces

- Accepted local placement at fixed relay ID `werkstattgasse:z1:relay:1`, plus a voluntary sanitized `?join=street&activation=...` invite carrying the complete verified, sign-only activation event and a fresh-browser reducer import path.
- A generic Phase-1 kind-9127 activation/witness/lens contract with exact ordered tags, strict parser, installed-signature verification, dependency authorization, raw/field/time/rate/dedup bounds, NIP-07 adapter, acknowledged publish, and relay-backed subscription filtering.
- Draft-first, explicit focused signer consent for activation, witness, and lens, with app-owned cancellation tokens, truthful cancellation/failure, silent EOSE/reconnect baselines, and accepted-delta-only presentation.
- A focused network-free `phase1-witness-security-smoke.ts` suite using `finalizeEvent` only to create deterministic signed fixtures and covering positive, negative, concurrency, UI, transport, and attribution invariants for all three actions.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship the complete signed activation invite and verified fresh-browser import</name>
  <files>apps/web/src/meaningverse/model.ts, apps/web/src/meaningverse/phase1Relay.ts, apps/web/src/ui/Phase1RelayOverlay.tsx, apps/web/src/scene/PalaceScene.tsx, apps/web/tests/phase1-witness-security-smoke.ts</files>
  <read_first>
    @.planning/phases/01-visible-art-in-werkstattgasse/01-CONTEXT.md (D-20, D-21, D-22)
    @.planning/phases/01-visible-art-in-werkstattgasse/01-PATTERNS.md (app truth separate from effects; phase-owned overlays; PalaceScene orchestration)
    @.planning/phases/01-visible-art-in-werkstattgasse/01-UI-SPEC.md (`OPEN`, voluntary invite, invited landing, keyboard/focus, copy, reduced-motion, long-text contracts)
    @apps/web/src/meaningverse/model.ts (`buildMeaningverseInvite` and invite-state helpers)
    @apps/web/src/meaningverse/phase1Relay.ts (accepted placement/activation reducer state)
    @apps/web/src/ui/Phase1RelayOverlay.tsx
    @apps/web/src/scene/PalaceScene.tsx
    @apps/web/tests/phase1-witness-security-smoke.ts
  </read_first>
  <behavior>
    - "Accepted local placement allocates exactly `werkstattgasse:z1:relay:1`; URL/query state cannot create activation truth, and accepted activation alone exposes restrained OPEN plus explicit Inspect relay/Copy invite UI (D-20, D-21)."
    - "Copy invite opens a focus-contained consent surface for action `activate-relay-invite`; only after callable `window.nostr.getPublicKey` and `window.nostr.signEvent` capability detection and explicit approval may signing begin."
    - "The sign path creates fresh `new NDKNip07Signer(1000, getNdk())`, awaits `blockUntilReady()`, creates `new NDKEvent(getNdk(), template)`, awaits `event.sign(signer,{skipContentTagging:true})`, reparses `event.rawEvent()`, applies strict shape checks, and calls installed `verifyEvent` before making an invite."
    - "The activation event has kind 9127, empty content, and exactly ordered two-string tags `t=palace-phase-1`, `action=activate-relay-invite`, `relay=werkstattgasse:z1:relay:1`, `creator=event.pubkey`; no unknown, duplicate, reordered, or extra tag is accepted."
    - "Activation capability is never published; the complete verified event becomes max-4096-byte base64url UTF-8 JSON in an allowlisted URL containing exactly `?join=street&activation=...`, with every other query parameter and hash removed."
    - "A fresh browser requires exactly join plus activation, decodes/parses the complete event, validates raw size, exact event shape/kind/content/tags, `creator===event.pubkey`, signature, no more than +60 seconds future skew, and no more than 15 minutes age, then and only then dispatches `importVerifiedActivationCapability`."
    - "The reducer import creates only bounded `{relayId,activationId,creatorPubkey,source:\"verified-invite-capability\"}` state authorizing lit/bypass/witness eligibility; invalid, expired, malformed, mutated, missing, or extra-route capability follows the canonical unlit creator route and never renders invited-lit UI."
    - "Missing or rejected signer, app-owned cancel, late resolve, malformed return, and any validation failure are fail-closed; URL display, clipboard success, selectable/manual fallback, `I copied it`, close, or cancellation never proves sharing or receipt."
    - "Opening moves focus to a heading/first safe control, focus remains contained, Tab/Shift-Tab/Enter/Space work, Escape closes the topmost non-pending panel or uses the active cancellation path, terminal close returns focus to the relay invoker, world E is suppressed while modal controls own focus, and long escaped URL text wraps/selects without unsafe HTML."
  </behavior>
  <action>
    RED: extend the network-free smoke first with deterministic activation-event fixtures and assertions for fixed relay allocation, no route-created truth, exact consent gating, callable NIP-07 detection, exact signer/NDKEvent call order, strict reparse plus installed verification, exact ordered activation tags, sign-only behavior, max-4096-byte base64url UTF-8 round-trip, query/hash allowlisting, 15-minute age/+60-second future boundaries, reducer import shape, canonical fallback, cancel/late-result invalidation, copy/manual truth, invited bypass, focus ownership, keyboard handling, reduced-motion compatibility, and long escaped-text/source contracts. Mock the signer/NDK boundary and browser URL/clipboard locally; make no network connection.

    GREEN: when local placement is accepted, allocate the fixed relay ID `werkstattgasse:z1:relay:1` in phase-owned state; never infer it from route data. Preserve `buildMeaningverseInvite` as the only URL builder but narrow its successful activation form to an allowlist of exactly `join=street` and `activation`, stripping all other query keys and the hash. Model explicit invite states covering idle, consent, signer-pending, copied, manual, failed, and cancelled, plus an app-owned monotonically unique attempt token. `Copy invite` opens consent for the bounded action `activate-relay-invite`; it does not immediately copy, sign, or publish. Capability-detect that `window.nostr`, `window.nostr.getPublicKey`, and `window.nostr.signEvent` exist and are callable. On approval use exactly a fresh `new NDKNip07Signer(1000, getNdk())`, await `blockUntilReady()`, construct `new NDKEvent(getNdk(), template)`, await `event.sign(signer,{skipContentTagging:true})`, then treat `event.rawEvent()` as unknown: serialize/reparse, enforce the exact standard event shape and 4096-byte cap, require exact activation bindings and `creator===pubkey`, and call installed `verifyEvent`. Encode that complete verified raw event as unpadded base64url of its UTF-8 JSON and pass it to the sanitized builder. Do not invoke publish for activation under any circumstance. An app-owned cancel invalidates the attempt token; ignore every late readiness/sign/validation/copy result whose token is no longer current (D-21).

    Implement fresh-browser parsing before invited rendering. Accept exactly `join=street` plus one `activation` value and no hash/other query; reject rather than normalize malformed base64url, invalid UTF-8/JSON, over-4096-byte decoded JSON, wrong/extra event fields, content, kind, tag shape/order/count/value, creator/pubkey binding, invalid signature, age beyond 15 minutes, or future skew beyond 60 seconds. Only a verified candidate may dispatch reducer action `importVerifiedActivationCapability`, whose stored value is bounded to `{relayId,activationId,creatorPubkey,source:"verified-invite-capability"}` and authorizes the same fixed lit relay, intro/feed bypass, and witness eligibility. Any invalid path uses the canonical unlit creator route, never partial invited state. On valid import, place the camera 3–5m from the same lit relay with immediate movement and retain `INVITED TO A LIT RELAY` plus `Touch the relay to answer. Nothing is sent until you approve a signature.` Arrival creates no witness draft or signer request (D-22, D-23).

    Preserve restrained `OPEN`, `Relay is OPEN`, `The light is on. Invite one person when you want to.`, `Copy invite`, `Not now`, selectable URL, `Copy the invite manually`, `I copied it`, `Close invite`, and truthful retry/failure copy. Clipboard success may say `Invite copied` but cannot mutate shared/received/witness truth. Preserve normal escaped React rendering, wrapping/selectable long text, focus containment/return, keyboard activation, fail-closed Escape/cancel, world-E suppression, and existing accessibility semantics. REFACTOR only after focused tests are green.
  </action>
  <verify>
    <automated>pnpm --filter @600b/web exec tsx tests/phase1-witness-security-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    Network-free tests prove accepted placement owns the fixed ID; explicit activation consent uses the exact fresh NIP-07/NDKEvent sequence; the exact signed activation event is verified, never published, and embedded in a <=4096-byte sanitized two-key URL; only exact fresh capability import creates bounded invited eligibility; invalid/mutated/expired/cancelled/late/copy-only paths stay canonical and unlit; retained focus, keyboard, reduced-motion, long-text, and truthful UI contracts pass (D-20 through D-23).
  </acceptance_criteria>
  <done>The creator can deliberately sign and copy one complete activation capability, and a fresh browser reaches the same lit relay only after strict capability verification and reducer import; neither route state nor any copy/sign failure creates truth.</done>
  <reversibility rating="reversible">The bounded in-memory capability, route allowlist, and UI attempt state can be removed without persisted-data migration, package change, or external schema change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD the generic exact activation, witness, and lens kind-9127 transport contract</name>
  <files>apps/web/src/meaningverse/phase1Relay.ts, apps/web/src/net/phase1RelayTransport.ts, apps/web/tests/phase1-witness-security-smoke.ts</files>
  <read_first>
    @.planning/phases/01-visible-art-in-werkstattgasse/01-CONTEXT.md (D-20, D-23, D-24)
    @.planning/phases/01-visible-art-in-werkstattgasse/01-RESEARCH.md (untrusted events, signature validation, replay, size/rate/freshness, fail-closed sequence)
    @.planning/phases/01-visible-art-in-werkstattgasse/01-PATTERNS.md (strict signed-event seam; transport isolation; smoke style; accepted delta)
    @.planning/phases/01-visible-art-in-werkstattgasse/01-VALIDATION.md (witness/lens security map)
    @apps/web/src/meaningverse/phase1Relay.ts
    @apps/web/src/net/phase1RelayTransport.ts
    @apps/web/tests/phase1-witness-security-smoke.ts
  </read_first>
  <behavior>
    - "Every action uses kind 9127 and empty content with strict exact ordered two-string tags; unknown actions and missing, duplicate, reordered, malformed, or extra tags/fields fail closed before authorization."
    - "Activation tags are exactly `t=palace-phase-1`, `action=activate-relay-invite`, `relay=werkstattgasse:z1:relay:1`, `creator=<event.pubkey>`; accepted local activation is the precondition, and activation is signed only, never published."
    - "Witness tags are exactly `t=palace-phase-1`, `action=touch-relay-witness`, `relay=werkstattgasse:z1:relay:1`, `e=<activationId>`, `p=<creatorPubkey>`; signer pubkey must differ from creator, activation/creator must match imported/local truth, and at most one witness can be accepted."
    - "Lens tags are exactly `t=palace-phase-1`, `action=attach-signal-lens`, `relay=werkstattgasse:z1:relay:1`, `e=<activationId>`, `p=<creatorPubkey>`, `w=<acceptedWitnessId>`; an accepted witness must already exist, lens signer must equal its pubkey, and at most one lens can be accepted."
    - "Unknown raw input must pass <=4096 UTF-8 bytes, exact standard Event fields, strict lowercase hex/integer/content/tag limits, installed `verifyEvent`, activation/creator/witness binding, evidence freshness <=300 seconds/+60 seconds future, 4/pubkey plus 16/global attempts per 60 seconds, 64-ID dedup, and current-transition authorization before reducer acceptance."
    - "Plan 04 owns generic templates, strict parser/validator, NIP-07 adapter contract, activation sign-only guard, witness/lens publish acknowledgement, scoped subscription/filter, raw/field bounds, rate/dedup state, and fixtures for all three actions in `phase1RelayTransport.ts`; Plan 05 consumes accepted lens evidence unchanged."
    - "Positive activation, witness, and lens fixtures pass; malformed shape/tag/action/binding/signature/freshness, rate, replay, pre-witness lens, creator-as-witness, second witness, second lens, and wrong lens-signer fixtures are rejected without reducer or presentation truth."
  </behavior>
  <action>
    RED: expand `phase1-witness-security-smoke.ts` before production code with deterministic `node:assert/strict` fixtures for all three actions. Create valid signatures only with the already-installed nostr-tools package-root `finalizeEvent` and deterministic test-only secret keys; prohibit `finalizeEvent`, test nsecs, and app-held/demo nsecs from production source. Add positive activation, witness, and lens cases and negatives for non-object/malformed JSON, missing/extra standard fields, unsafe integers, uppercase/wrong-length/non-hex id/pubkey/sig, raw JSON at 4096 and just over 4096 UTF-8 bytes, non-empty/oversized content, malformed tuple/tag count/key/value, unknown action, and every missing/duplicate/reordered/extra/wrong tag. Cover bad id/signature, stale at/over 300 seconds, future at/over +60 seconds, wrong relay/activation/creator/witness binding, creator-as-witness, lens before witness, lens signed by anyone except the accepted witness pubkey, replay/duplicate, fifth per-pubkey and seventeenth global attempts in 60 seconds, simultaneous witness candidates, second witness, simultaneous lens candidates, and second lens. Assert every failed path leaves creator, OPEN, accepted IDs, witness, lens, and presentation deltas unchanged.

    GREEN: in `phase1RelayTransport.ts`, define the generic discriminated action contract and bounded template constructors for `activate-relay-invite`, `touch-relay-witness`, and `attach-signal-lens`. Use the fixed relay ID and exact ordered tags stated in behavior; all content is exactly empty. Activation template construction requires accepted local activation and derives `creator` from the signer pubkey. Witness derives `e` and `p` only from accepted activation truth and requires the candidate signer pubkey differ from creator. Lens derives `e`, `p`, and `w` only from accepted activation plus accepted witness truth and requires its signer pubkey equal the accepted witness pubkey. Never accept caller-supplied replacements for trusted bindings. Add generic NIP-07 signing interfaces and publisher/subscriber contracts here for all actions; encode in the type/implementation that activation may be signed/exported but publish rejects it, while only witness/lens may use acknowledged publish and relay-backed receive. Define the exact `NDKFilter<number>` builder as `{kinds:[9127],"#e":[activationId],since:activationCreatedAt-60,limit:16}` and generic callbacks carrying relay provenance, optimistic-publish status, EOSE/reconnect epoch, and disposal.

    Parse unknown serialized/raw event input with a UTF-8 byte guard of 4096 before object allocation beyond the bounded parser. Permit exactly the standard Event fields `id`, `pubkey`, `created_at`, `kind`, `tags`, `content`, and `sig`, with no extras; require kind 9127, empty content, lowercase 64-hex id/pubkey, lowercase 128-hex sig, non-negative safe-integer Unix seconds, bounded tag tuples containing exactly two strings, bounded key/value lengths, and the exact action-specific tag counts/order/values. Select conservative explicit limits no looser than key 16 UTF-8 bytes and value 128 UTF-8 bytes, reject rather than truncate/coerce, and test every boundary. Only after strict shape parsing call package-root `verifyEvent` from nostr-tools. Then apply trusted activation/creator/witness dependency binding, freshness no older than 300 seconds and no more than 60 seconds future skew for witness/lens live evidence, per-pubkey rate 4 and global rate 16 per rolling 60 seconds, and a bounded 64-event-ID dedup structure. Keep rate windows and dedup deterministic and bounded; duplicate/rate failures cannot authorize a transition. Activation invite import retains Task 1's separate <=15-minute/+60-second capability window because it is transported directly rather than accepted as live relay evidence.

    In `phase1Relay.ts`, reduce only a typed verified-and-authorized result. Preserve local/imported activation provenance and exact `{relayId,activationId,creatorPubkey}` dependency keys. Serialize transitions so at most one witness and then one lens are accepted; simultaneous candidates produce one accepted reducer delta and idempotent no-ops. Retain accepted witness ID/pubkey for lens authorization and retain accepted IDs for the bounded session so eviction cannot permit reacceptance. Expose immutable accepted lens evidence as the Plan-05 handoff without transforming, enriching, or reclassifying it. A valid signature proves key control for that event only. REFACTOR after all positive, negative, boundary, rate, replay, and concurrency tests pass.
  </action>
  <verify>
    <automated>pnpm --filter @600b/web exec tsx tests/phase1-witness-security-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    Tests demonstrate all three exact action schemas, standard-field/raw/tag/hex/time boundaries, installed signature verification, activation/creator/witness dependency binding, 300-second/+60-second live freshness, 4/pubkey and 16/global per-60-second limits, 64-ID dedup, and serialized one-witness/one-lens reducer authorization; all malformed, binding, signature, freshness, rate, replay, pre-witness, creator-as-witness, second-witness, second-lens, and wrong-signer negatives are no-ops, and `finalizeEvent` exists only in test fixtures (D-20, D-23, D-24).
  </acceptance_criteria>
  <done>`phase1RelayTransport.ts` provides one exact generic contract for activation, witness, and lens, and no input can change reducer truth until structural, cryptographic, contextual, temporal, rate, replay, dependency, and transition gates all succeed.</done>
  <reversibility rating="reversible">The Phase-1 in-memory contract, validator, and bounded controls can be replaced behind their adapter without persisted-data migration; any relaxation requires explicit security review.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire exact NIP-07 sign, acknowledged witness/lens publish, and relay-backed receive</name>
  <files>apps/web/src/meaningverse/model.ts, apps/web/src/meaningverse/phase1Relay.ts, apps/web/src/net/phase1RelayTransport.ts, apps/web/src/ui/Phase1RelayOverlay.tsx, apps/web/src/scene/PalaceScene.tsx, apps/web/tests/phase1-witness-security-smoke.ts</files>
  <read_first>
    @.planning/phases/01-visible-art-in-werkstattgasse/01-CONTEXT.md (D-20, D-21, D-22, D-23, D-24)
    @.planning/phases/01-visible-art-in-werkstattgasse/01-PATTERNS.md (draft before signature; silent epoch; transport/scene isolation; accepted-delta feedback)
    @.planning/phases/01-visible-art-in-werkstattgasse/01-UI-SPEC.md (invite/witness/lens consent, pulse/replay copy, keyboard/focus/reduced-motion)
    @apps/web/src/meaningverse/model.ts
    @apps/web/src/meaningverse/phase1Relay.ts
    @apps/web/src/net/phase1RelayTransport.ts
    @apps/web/src/ui/Phase1RelayOverlay.tsx
    @apps/web/src/scene/PalaceScene.tsx
    @apps/web/tests/phase1-witness-security-smoke.ts
  </read_first>
  <behavior>
    - "Production uses package-root imports `NDKEvent`, `NDKNip07Signer`, `NDKPublishError`, and type `NDKFilter` from `@nostr-dev-kit/ndk`, plus package-root `verifyEvent` from `nostr-tools`; no app-held/demo nsec, test finalizer, custom crypto, deep SDK import, or direct scene/UI SDK call is allowed."
    - "Entering Street, opening/copying an invite, arriving invited, proximity, presence, participant count, subscription callback, or reconnect never constructs a witness/lens draft, requests signing, publishes, or accepts evidence (D-21, D-23)."
    - "Only an eligible invited-player `E · Touch relay` opens focused witness consent, and only its approval creates a bounded witness draft; lens consent is similarly available only to the accepted witness after witness truth exists."
    - "Each approved witness/lens attempt capability-detects callable NIP-07 methods, creates fresh `new NDKNip07Signer(1000, getNdk())`, awaits `blockUntilReady()`, builds `new NDKEvent(getNdk(), template)`, awaits `event.sign(signer,{skipContentTagging:true})`, reparses `event.rawEvent()`, then repeats strict shape, installed `verifyEvent`, and trusted dependency binding before publish."
    - "Only witness/lens call `await event.publish(undefined,3000,1,{skipContentTagging:true})`; acceptance requires the returned relay set to be non-empty, and `NDKPublishError`, timeout, rejection, empty acknowledgement, missing signer, app cancellation, or sign-only success leaves OPEN unchanged."
    - "A retry may republish the same still-fresh, already verified event, but cannot rebuild trusted bindings, bypass expiry, or accept on acknowledgement alone; reducer acceptance waits for that exact event to arrive through an eligible relay-backed live callback."
    - "The live subscription uses exact `NDKFilter<number>={kinds:[9127],\"#e\":[activationId],since:activationCreatedAt-60,limit:16}`, treats EOSE as a silent baseline, ignores cache-only, `optimisticPublish===true`, and no-relay callbacks, repeats all receive gates, and stops after accepted witness plus lens or disposal."
    - "App cancellation invalidates the attempt token and ignores late signer/readiness/sign/publish/receive results; missing/rejected signer, publish failure, reconnect, replay, malformed input, and unaccepted callbacks leave OPEN unchanged."
    - "Only an accepted reducer delta presents one 900ms cyan witness pulse or the accepted lens state; initial/reconnect baseline, publish acknowledgement, duplicate/replay, and already-accepted explicit touch produce no new acceptance pulse, with reduced-motion static/text equivalents and immutable creator attribution."
  </behavior>
  <action>
    RED: add network-free fake NDK/NIP-07/relay contract tests before wiring production. Assert production source imports named SDK APIs only from package roots and contains no nsec/test-finalizer/custom signing path or scene/UI SDK import. Prove no eager signer/draft/publish/subscription truth, exact capability detection and fresh signer readiness/sign/reparse/verification order, action-specific template binding, activation no-publish, exact witness/lens publish call arguments, non-empty relay acknowledgement requirement, `NDKPublishError`/timeout/empty-set failure, still-fresh same-event retry, exact filter object, EOSE/reconnect silence, and ignore rules for cache, optimistic, no-relay, duplicate, replay, malformed, wrong-bound, stale, and cancelled/late callbacks. Prove a relay-backed live receive repeats raw bounds through reducer authorization; one accepted witness delta produces one pulse, one accepted lens delta produces one lens handoff/presentation, and stop/dispose prevents later callbacks.

    GREEN: in `phase1RelayTransport.ts`, use exact package-root production imports: named `NDKEvent`, `NDKNip07Signer`, and `NDKPublishError`, plus type `NDKFilter`, from `@nostr-dev-kit/ndk`, and `verifyEvent` from `nostr-tools`. Use the existing application `getNdk()` seam. Keep all SDK values inside the net adapter and expose application-facing commands/results only; scene, overlay, model, and reducer must not import or receive mutable SDK objects. Do not place any nsec, deterministic fixture signer, or `finalizeEvent` in production. For every explicit activation/witness/lens consent attempt, first require callable `window.nostr.getPublicKey` and `window.nostr.signEvent`; create fresh `new NDKNip07Signer(1000, getNdk())`, await `blockUntilReady()`, create `new NDKEvent(getNdk(), template)`, await `event.sign(signer,{skipContentTagging:true})`, serialize/reparse `event.rawEvent()` as unknown, then run Task 2's strict parser, installed `verifyEvent`, freshness, and action/dependency binding. Activation returns the verified capability to Task 1 and can never enter publish. Witness/lens proceed only if eligible and verified.

    Publish witness/lens with exactly `await event.publish(undefined,3000,1,{skipContentTagging:true})`; retain the still-fresh verified raw event and require the resolved relay set to be non-empty before showing truthful submitted/waiting state. Catch `NDKPublishError`, timeout, rejection, and empty acknowledgement as fail-closed publish failure; sign success alone is never accepted. Retry may republish the same verified raw event only while it remains within the 300-second/+60-second policy, and must not re-sign or substitute event identity merely to force acceptance. Publish acknowledgement authorizes no reducer mutation: acceptance waits for the exact ID through relay-backed receive.

    Create the relay-backed live subscription only after local/imported accepted activation truth, using exactly `const filter: NDKFilter<number>={kinds:[9127],"#e":[activationId],since:activationCreatedAt-60,limit:16}`. Carry relay provenance into callbacks and accept only live callbacks backed by at least one relay; ignore cache data, callbacks where `optimisticPublish===true`, and callbacks without relay provenance. Treat initial EOSE and each reconnect as a silent baseline; neither history nor publish loopback presents or accepts by itself. For every eligible live unknown value, repeat raw 4096-byte bound, strict exact parser, `verifyEvent`, trusted activation/creator/witness binding, freshness, rate, 64-ID dedup, and reducer transition authorization. Stop the subscription after both witness and lens are accepted or on explicit disposal/ineligibility/unmount. Reconnect may establish a new silent baseline but cannot replay accepted IDs.

    In `Phase1RelayOverlay`, keep invited `E · Touch relay` as the sole witness entry. It opens the focus-contained `SIGNED LIGHT PULSE` sheet with `Send one light pulse?`, the bounded action explanation, `Sign pulse`, and `Not now`; only `Sign pulse` starts the exact adapter flow. Pending retains `Waiting for your signer…` and active `Cancel signing`; Escape uses the same cancellation token invalidation. Cancellation says `No pulse was sent. The relay stays OPEN.` Sign/validation/publish failure says `The pulse could not be verified. Nothing changed.` Acknowledged publish may truthfully indicate waiting for relay-backed confirmation but not acceptance. Add the similarly focused eligible lens action only after accepted witness state, using the existing UI-SPEC lens wording and the same cancellation/failure discipline; no pre-witness lens control may sign or publish. Every terminal close returns focus to the invoking relay control (D-23, D-24).

    In scene orchestration, start the narrow subscription only from accepted local activation or verified imported activation capability and tear it down on ineligibility/unmount. Maintain a unique app-owned token per consent attempt; Cancel, Escape, replacement attempt, and dispose invalidate it, and every late signer/readiness/sign/publish/receive continuation checks it before UI changes. Receive only application-facing accepted reducer deltas. Preserve `diffPhase1WitnessEpoch`: null initial state, EOSE, and reconnect establish silent baseline; only a newly accepted relay-backed witness delta shows `Pulse accepted`, `Your signed light reached this relay.`, and one 900ms cyan path, with reduced-motion static/text equivalent. An explicit later touch may show `Already witnessed · no new pulse sent`. Present lens only from its accepted reducer delta and pass the accepted lens evidence unchanged to Plan 05. Keep restrained OPEN on every non-accepted path, creator attribution byte-for-byte unchanged, and bounded event/pubkey attribution selectable, safely wrapping, escaped, and free of identity/humanity overclaim (D-20 through D-24). REFACTOR after the focused suite passes.
  </action>
  <verify>
    <automated>pnpm --filter @600b/web exec tsx tests/phase1-witness-security-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    Network-free tests prove exact package-root imports and NIP-07/NDKEvent call order, no production nsec/finalizer, explicit eligible consent, app-owned late-result cancellation, activation sign-only, exact acknowledged witness/lens publish, empty/error/timeout failure, exact scoped filter, relay-backed live receive with EOSE/reconnect/cache/optimistic/no-relay silence, repeated validation gates, accepted-delta-only pulse/lens presentation, immutable attribution, and clean stop/dispose behavior (D-20 through D-24).
  </acceptance_criteria>
  <done>A real eligible participant can deliberately sign, publish, and relay-confirm one bounded witness and then one bounded lens; every missing, cancelled, rejected, unacknowledged, non-relay-backed, malformed, stale, replayed, reconnect, or late path leaves OPEN and reducer truth unchanged.</done>
  <reversibility rating="reversible">The NIP-07 and relay implementation remains a replaceable net adapter over bounded in-memory Phase-1 truth, with no package, schema, persistence, or creator-identity migration.</reversibility>
</task>

</tasks>

<assumption_delta_decision>
Primary noun: signed Nostr activation, witness, and lens event identities.
Decision: no-change.
Rationale: A verified activation signature establishes control of its creator key for one bounded invite capability; a verified witness or lens signature establishes control of its signer key for that one dependency-bound action. Publish acknowledgement and relay-backed receipt add transport evidence, not human or account verification. None establishes a verified human, authenticated session handle, account ownership, or broader Palace identity. Imported activation therefore stores only `{relayId,activationId,creatorPubkey,source:"verified-invite-capability"}`; accepted witness and lens add only bounded event ID/pubkey attribution and dependency evidence. They cannot replace, merge, rename, transfer, or reclassify the creator or primary maker attribution. `phase1-witness-security-smoke.ts` snapshots creator identity/attribution, imports a valid activation, accepts a distinct witness and witness-signed lens, and asserts creator fields remain byte-for-byte unchanged while only the bounded activation/witness/lens records are added. Plan 05 consumes the accepted lens record unchanged and gains no identity authority.
</assumption_delta_decision>

<threat_model>
ASVS target: L1 baseline. Every high-severity item below has disposition `mitigate`, is blocking, and must have focused automated evidence green before this plan completes.

## Trust Boundaries

| Boundary | Untrusted side -> trusted side | Exact rule |
|---|---|---|
| TB-01 Local placement/URL -> activation truth | Scene action or browser address -> phase reducer | Accepted local placement allocates `werkstattgasse:z1:relay:1`; URL state never creates truth, and fresh browser import requires exact verified activation capability. |
| TB-02 NIP-07 availability/result -> verified event | Browser extension methods and returned event -> net adapter | Capability-detect callable methods, use fresh signer readiness/sign, then reparse strict raw event and run installed `verifyEvent`; cancellation/late result has no authority. |
| TB-03 Activation event -> invite URL/import | Verified raw event -> base64url query -> reducer | Never publish; cap decoded JSON at 4096 bytes, allow exactly join+activation/no hash, validate exact bindings/signature and 15-minute/+60-second window before bounded import. |
| TB-04 Relay callback -> bounded parser | Arbitrary callback value -> phase-owned candidate | Require relay-backed live provenance, reject cache/optimistic/no-relay, enforce 4096-byte raw limit and exact standard fields/tags before verification or scene allocation. |
| TB-05 Parsed event -> installed signature verification | Strict candidate -> nostr-tools `verifyEvent` | Use package-root installed verification only; no custom crypto, cast-as-proof, test finalizer, app nsec, or invented/deep API. |
| TB-06 Verified event -> contextual authorization | Valid signature -> Phase-1 evidence | Enforce exact action schema, activation/creator/witness dependencies, freshness, rate, 64-ID dedup, and one-witness/one-lens transition. |
| TB-07 Verified witness/lens -> publish acknowledgement | Signed event -> relay publish result | Call exact bounded publish, require non-empty relay set, and fail closed on NDKPublishError/timeout/empty; acknowledgement is not reducer acceptance. |
| TB-08 Publish/subscription -> relay-backed receive | SDK event/epoch -> app adapter | Exact activation-scoped filter, EOSE/reconnect silence, relay provenance, repeated full validation, and accepted-delta-only reducer/presentation. |
| TB-09 Consent/UI/scene -> SDK adapter | Eligible E plus focused approval -> external signer | UI/scene pass bounded commands and attempt tokens only; no SDK import, eager signing, direct truth, or late-result update. |
| TB-10 Accepted attribution -> DOM/Plan 05 | Event IDs/pubkeys -> visible text/lens handoff | Render bounded escaped wrapping/selectable text, no unsafe HTML/event URL/identity overclaim; pass immutable accepted lens evidence unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Specific mitigation |
|---|---|---|---|---|---|
| T-01-04-S1 | Spoofing | Activation invite / fresh browser | high | mitigate | Exact event fields/tags, `creator===pubkey`, installed verification, 4096-byte cap, strict route allowlist, 15-minute/+60-second import window, and canonical unlit fallback tests. |
| T-01-04-S2 | Spoofing | Witness/lens signer and dependencies | high | mitigate | Exact ordered schemas, creator-as-witness rejection, lens signer equals accepted witness pubkey, activation/creator/witness binding, and wrong-signer/pre-witness tests. |
| T-01-04-T1 | Tampering | Generic parser/validator | high | mitigate | Exact standard fields, kind 9127, empty content, exact action tag counts/order/values, strict hex/integer/byte limits, reject extras/coercion, and installed `verifyEvent`. |
| T-01-04-T2 | Tampering | URL/query capability | high | mitigate | Complete event in unpadded base64url UTF-8 JSON, exact join+activation/no-hash allowlist, round-trip/reparse, mutation/extra-query negative tests, and route never creates truth. |
| T-01-04-T3 | Tampering | Concurrent transitions | high | mitigate | Serialized reducer acceptance with retained activation/witness/lens IDs; simultaneous and second-witness/second-lens tests prove exactly one each. |
| T-01-04-R1 | Repudiation | Sign/publish/receive outcomes | medium | mitigate | Distinguish cancelled, signed, verified, publish-acknowledged, relay-received, and reducer-accepted states; only bounded accepted IDs/pubkeys are retained, without humanity claims. |
| T-01-04-R2 | Repudiation | Copy/sharing status | medium | mitigate | Display/copy/manual/close/cancel states never set shared/received truth; activation capability proves key control, not delivery to another person. |
| T-01-04-I1 | Information disclosure | Drafts, URL, attribution DOM | medium | mitigate | Empty content, public bounded tags only, no private key/demo nsec, sanitized URL, escaped selectable text, no unsafe HTML/event navigation/guessed identity. |
| T-01-04-D1 | Denial of service | Raw parser and verifier | high | mitigate | 4096-byte precheck, exact bounded fields/tags, 300-second/+60-second live freshness, 4/pubkey and 16/global per 60 seconds, 64-ID dedup, and boundary/burst tests. |
| T-01-04-D2 | Denial of service | Subscription/reconnect/replay | high | mitigate | Exact activation-scoped limit-16 filter, silent EOSE/reconnect baseline, cache/optimistic/no-relay rejection, retained accepted IDs, and stop after witness+lens/dispose. |
| T-01-04-E1 | Elevation of privilege | Sign/publish callback | high | mitigate | Exact NIP-07 sequence, strict reparse/verify/binding, non-empty publish acknowledgement, app-owned cancellation token, and no sign/publish callback directly dispatching acceptance. |
| T-01-04-E2 | Elevation of privilege | Scene/UI/mock/presence | high | mitigate | SDK isolation in net adapter; bot/Kerni/timer/count/backlog/ambient/loopback/presence/cache cannot create drafts or truth; source-contract and no-fake-input tests. |
| T-01-04-E3 | Elevation of privilege | Lens handoff/creator identity | high | mitigate | Lens requires accepted witness and same witness signer, immutable creator invariant, bounded additive attribution, and unchanged accepted evidence handoff to Plan 05. |
</threat_model>

<verification>
Run from the repository root after each task:

1. `pnpm --filter @600b/web exec tsx tests/phase1-witness-security-smoke.ts`

Run the complete plan gate after Task 3:

1. `pnpm --filter @600b/web exec tsx tests/phase1-tracer-smoke.ts`
2. `pnpm --filter @600b/web exec tsx tests/phase1-presence-smoke.ts`
3. `pnpm --filter @600b/web exec tsx tests/phase1-placement-smoke.ts`
4. `pnpm --filter @600b/web exec tsx tests/phase1-witness-security-smoke.ts`
5. `pnpm --filter @600b/web exec tsx tests/meaningverse-smoke.ts`
6. `pnpm --filter @600b/web exec tsx tests/multiplayer-smoke.ts`
7. `pnpm --filter @600b/web typecheck`
8. `pnpm --filter @600b/web test`

The focused suite is network-free and must cover fixed-ID local activation, exact signed activation invite round-trip/import and canonical fallback, exact NIP-07 call order, activation sign-only, all three exact ordered action schemas, raw/field/tag/hex/time limits at-boundary and just-over-boundary, installed `verifyEvent`, activation/creator/witness dependencies, acknowledged witness/lens publish, exact filter and relay-backed receive, cancellation/late resolution, malformed/mutated/extra/oversized/stale/future/wrong-binding/invalid-signature input, rate/replay/duplicate/burst, pre-witness and creator-as-witness rejection, second/simultaneous witness/lens idempotency, silent EOSE/reconnect/cache/optimistic/no-relay handling, accepted-delta-only presentation, no-fake-input evidence, focus/keyboard/long-text/reduced-motion behavior, creator-attribution invariance, and Plan-05 lens handoff immutability. `finalizeEvent` is allowed only in deterministic test fixtures. All high threats are blocking.
</verification>

<success_criteria>
- Accepted local placement owns `werkstattgasse:z1:relay:1`; route state cannot create activation, witness, or lens truth (D-20).
- The creator voluntarily chooses Copy invite and approves exact NIP-07 activation signing; the complete verified exact activation event is <=4096 bytes, never published, and embedded in an allowlisted `?join=street&activation=...` URL, while copy/manual/cancel/failure never claims delivery or sharing (D-21).
- A fresh browser imports only an exact, valid, <=15-minute, at-most +60-second-future capability and stores bounded verified provenance; every invalid/mutated/expired/incomplete/extra route follows canonical unlit creator UI, while valid import reaches the same lit relay and bypasses intro/feed without auto-signing (D-22).
- Generic kind-9127 activation, witness, and lens templates/parser/validator use exact ordered tags, empty content, strict standard fields, installed `verifyEvent`, dependency binding, 300-second/+60-second live freshness, 4/pubkey plus 16/global per-60-second limits, and 64-ID dedup before reducer authorization.
- Only explicit eligible focused approval requests a fresh NIP-07 signer; witness/lens use exact bounded publish and require a non-empty relay acknowledgement, but signer/publish success alone never accepts (D-23, D-24).
- Exact activation-scoped subscription receives relay-backed live evidence, treats EOSE/reconnect as silent baseline, ignores cache/optimistic/no-relay callbacks, repeats all validation gates, and stops after one accepted witness plus one accepted lens or disposal.
- App cancel invalidates attempt tokens; missing/rejected signer, malformed return, NDKPublishError, timeout, empty acknowledgement, reconnect, replay, duplicate, wrong dependency, pre-witness lens, creator witness, second witness/lens, and late result leave OPEN unchanged.
- Only accepted reducer deltas produce one witness pulse and one accepted lens presentation/handoff; Plan 05 receives accepted lens evidence unchanged, and no fake peer, presence, callback, URL, signer, publish, or presentation effect can fabricate truth.
- URL and bounded attribution wrap/select safely as escaped React text; creator identity/attribution remains byte-for-byte unchanged, with no identity or humanity overclaim; focus containment/return, keyboard/Escape, cancellation, and reduced-motion contracts remain intact.
- No package, deep/invented SDK API, production nsec/finalizer, custom crypto, schema, unsafe HTML, broad persistence/multiplayer/social feature, direct scene/UI SDK use, or direct callback truth is introduced.
- Focused, tracer, presence, placement, meaningverse, multiplayer, typecheck, and web test gates all pass.
</success_criteria>

<output>
Create `.planning/phases/01-visible-art-in-werkstattgasse/01-04-SUMMARY.md` when done.
</output>
