packages:
  - "apps/*"
  - "packages/*"
allowBuilds:
  "@biomejs/biome": true
  better-sqlite3: true
  esbuild: true
  msgpackr-extract: true

# services/world-agent is a Python service (uv), intentionally NOT a pnpm workspace member.
